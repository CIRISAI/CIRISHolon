# Pre-registration — SATURATION-2: water, emergent — the bent molecule is a three-body fact

*Frozen 2026-08-30, committed ALONE. SATURATION-1 proved hydrogen's valence
emerges when the third body pays; its scope fence named heteronuclear
three-body tables as the successor. This campaign lands the first one: the
(O, H, H) surface, exact-in-model STO-3G FCI (a water point is 441
determinants), tabulated and differentiated into the sandbox exactly as
H3's was — so that H2O's GEOMETRY (bent), its VALENCE (exactly two
hydrogens), and its FORMATION (from a quenched H + O gas) emerge from the
same three declared inputs, with no molecular preset anywhere. The lead's
standing order against manual molecules is this freeze's premise: a
molecule you place is a fake; a molecule the ledger pays for is a result.*

misfits: contacts M-ONE-MODEL-DELTA (exact-in-model; nature's 104.5
degrees and 0.957 angstrom appear as labelled context only),
M-STALE-INSTRUMENT (referee and table producers committed with their
outputs), M-PLANT-OBS and M-PLANT-SECTOR (plants below), M-NULL-MISSTAKE
(the saturation stake is on in-model quantities), M-PARITY-PROTECT (the
spin audit carries per geometry; water's sector is a singlet, the OH
fragment a doublet — asserted where resolved, reported where degenerate),
M-HOMOG, M-FINAL-VIEW-COLLISIONS, M-NONBIJECTIVE-STEP,
M-FIXED-POINT-TRAJECTORY, M-PROBE-EIGENSTATE, M-GAUGE-LAUNDER,
M-LOOP-BLIND, M-BARE-CHARGE, M-COND-PROBE, M-ELECTRIC-BASIS,
M-RING-MIXING, M-GAUGE-UNIFORM-MOMENTUM, M-KINEMATIC-NONLOCAL,
M-VOLUME-SCALE (not otherwise contacted).

## Scope, stated before the gates

One heteronuclear triple type: (O, H, H). The table's exact symmetry is
H <-> H exchange ONLY (oxygen is distinct; sorting runs over the two O-H
sides, never all three). Triples of type (O, O, H) and (O, O, O) are NOT
tabulated and run pair-only with the fence DISPLAYED and the truncation
REPORTED wherever gate P1's scenes produce them; O2 bonding through its
pair curve is legitimate MBE2 and labelled as such. Depends on MIXTURES-1's
pair bank for the sandbox gates (B-side); the table and its referee do not
wait for it. The four standing questions bind every gate here (connected
in the place it runs; sweep reaches where the claim fails; nothing-reads
audit with the writer outside the reader set; a demonstrated failing case
before trust).

## Gates

- **R1 — the 50-digit referee**: engine f64 E(H2O) and dE3(O,H,H) match an
  independent mpmath referee (extending the committed elements1_referee
  machinery) at <= 1e-10 Ha on >= 48 staked geometries spanning the domain
  (compact, bent, linear, stretched, near-boundary), rule stated
  result-blind in the file. witness: none (measured)
- **T1 — interpolant fidelity, held out**: 256 held-out geometries, staked
  seed, none on nodes; max error REPORTED, kill > 1e-3 Ha; nonzero
  required (two-sided). T2: the domain-boundary systematic <= 1e-5 Ha per
  AMENDMENT-A1's second-smallest-side logic adapted to the two O-H sides.
  witness: none (measured)
- **G1 — THE EMERGENT GEOMETRY**: minimizing pairs-plus-dE3 over the
  three-parameter space reproduces the model's OWN full-FCI optimal water
  geometry (computed directly, it is cheap) — bond length and angle each
  within the MBE3 truncation error T1 measures, both deviations REPORTED;
  the full-FCI in-model optimum is the reference, nature's geometry is
  labelled context beside it. Kill: an MBE3 optimum qualitatively wrong
  (linear where FCI is bent) kills the truncation's fitness for this
  triple and the campaign says so. witness: none (measured)
- **G2 — VALENCE SATURATION**: in-model, the third hydrogen refuses — the
  best E(H3O) binding of a third H to relaxed in-model water is shallower
  than water's own second O-H bond by a REPORTED factor, staked >= 5x
  (branch (b): investigate, never massage). The two-sided control: the
  SECOND hydrogen must bind deeply (that is water existing at all).
  witness: none (measured)
- **C1 — conservation with the heteronuclear triple paying**: energy and
  momentum gates, one per law, drift <= derived bounds with the (O,H,H)
  stiffness in the envelope and per-species masses. witness: none
  (measured)
- **P1 — THE PRODUCT: water forms**: the frozen quench protocol (staked
  and committed BEFORE the mixed arm runs: 8 H + 4 O, 8 seeds, thermostat,
  controls = pure-H arm reproducing SATURATION-1's molecules and pure-O
  arm reproducing O2 pair behaviour) ends branch (a): H2O is the modal
  O-containing molecule, zero free O. Branch (b) reported and
  investigated; VOID if a control fails. The (O,O,H) fence's incidence in
  these runs is COUNTED and reported. witness: none (measured)

## plants (carrier and sector per M-PLANT-SECTOR)

Each plant's carrier is asserted nonzero in the sector the plant acts on
before the plant is scored; a plant on an empty sector VOIDs.

- **(i) the sign flip**: negating the (O,H,H) table must invert G1's
  bent-vs-unbent preference (carrier: the geometry shift, asserted large).
- **(ii) the symmetry plant**: H <-> H exchange must be EXACT (bit-level)
  on a staked asymmetric geometry, and a deliberately desymmetrized table
  must fire >= 1e-6 Ha; the O-distinct axis must NOT be symmetrized — a
  table wrongly symmetrized over all three sides must fire against the
  referee (carrier: both deviations).
- **(iii) the swapped-table plant**: serving the (H,H,H) table to an
  (O,H,H) triple must fire G1 beyond tolerance by orders (carrier: the
  energy shift).
A missed plant VOIDs.

## Meaning

All gates => "the first heteronuclear molecule — its bent shape, its
exactly-two hydrogens, and its formation in a cooling gas — emerges from
Z, masses, and a basis through the same ledger that made H2, with the
model's own full CI as referee at every step and nature's numbers standing
beside the record as labelled context." NOT claimed: (O,O,H)/(O,O,O)
completeness (fence displayed and counted), quantitative agreement with
nature's 104.5 degrees (STO-3G's in-model angle is the claim), liquid
water, hydrogen bonding, or anything at scale.
