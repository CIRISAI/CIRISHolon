# Emitted keys that nothing reads and nothing checks, each one deliberate
# human provenance.  This file is SEPARATE from the auditor because naming a
# field inside the auditor would put it in the auditor's own text and launder
# it out of the inert bucket -- which is how ELEMENTS-1's audit came to report
# every one of its fields as read.
#
# Anything not on this list that lands in the inert bucket fails the audit.
# The bar for adding a line here is that the field is written FOR A PERSON and
# would be meaningless to a program.

staked_eight_note    # why present+owed is the invariant and not the file list
no_d_shell           # the argument, with the prereg's own arithmetic in it
grid_rule            # what a grid is a function of, and what it is blind to
d1_bridge_note       # what the 1e-8 gate means and which side a failure is on
why_it_matters       # what a vacuous coverage pass would look like
role                 # what a D1 bridge reference file is for
kill                 # which claim a D1 disagreement takes down, and which not
stake                # gate E1's stake, restated where the negatives are listed
precision            # why 50 digits against a 1e-8 gate is not overkill
