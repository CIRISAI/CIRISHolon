# GF2a — Amendment A3: the exam's ceiling per sector, from the rank A2 already used for the ladder

*Frozen 2026-09-03, committed ALONE before its runs, against `GF2A_AMENDMENT_2.md` (frozen
2026-09-03, ADMITTED) and the A2 exam section of `GF2A_QCD2_RESULTS.md`. G0″ FIRED at
χ = 256 on x = 4 B = 0, x = 9 B = 1 and x = 9 B = 0 and stays fired there. This amendment
changes ONE thing A2.3 got wrong against A2.6: the exam's χ was flat at 256 for every sector
while A2.6 derives the ladder's χ from the rank of the cut. Every other digit, gate and
plant of A2 stands.*

misfits: contacts M-UNTESTED-GAP (A2.3 staked 1e-6 at a χ its own rank table said was 6.2×
and 16× under the cut on the sectors that fired; the cold and mixed routes agreeing to
1e-7 there is the floor's signature, and this freeze stakes the ceiling where the counting
puts it), M-MAX-OVER-SUCCESSES (the χ per sector is derived from the rank, not raised until
a pass appears), M-TRUNCATION-AS-ERRORBAR and M-EXIT-DISCRIMINATOR (unchanged from A2: the
variance is the error bar and was measured ordered with the miss on all six sectors; the
exit is recorded and read), M-PLANT-OBS and M-PLANT-SECTOR (no plant changes; the carriers
of A2.8 stand, nonzero in the sector each plant acts on), M-STALE-INSTRUMENT (binaries by
hash in the results document), M-DEVICE-CLASS (class per row), M-CHEAPER-THAN-ITS-PRICE
(cost per rung printed), M-BUDGET-LAUNDER (a rung the lease refuses VOIDs by name),
M-VOLUME-SCALE (N = 8 remains an exam, never a volume). Not contacted: the rest.

defects: none.

gauge: /home/emoore/CIRISHolon/conformance/crystal/GF2A_QCD2_RESULTS.md — the A2 exam
section: three sectors firing at χ = 256 with cold = mixed to 1e-7, and three passing to
1e-11 or 4e-7 where χ sits within 1.4× or 6.2× of the rank.

Family-wise correction: none applied, none owed — separable verdicts at staked digits.

## A3.1 G0‴ — the exam's gate, χ per sector (EXACT)

At N = 8, both x, the MIXED warm ladder runs to a ceiling set by the cut's rank: B = 2 to
χ = 256 (rank 366), B = 1 to χ = 512 (rank 1,582), B = 0 to χ = 1,024 (rank 4,096), on the
rungs 64 → 128 → 256 (→ 512 → 1,024 as needed), α = 1e-4 with A2's schedule. The gate:
`|E₀(exact arm) − E₀(MPS)| ≤ 1e-6` at the ceiling, and the cold start at the ceiling within
1e-6 of it. A sector that misses either fires G0‴ for the arm; the ceiling is not raised
without a further amendment. Digit-bearing: 1e-6, 256, 512, 1024, 1e-4. witness: none (a
cross-solver identity).

## A3.2 What stands

V1 (A2.4), R1 (A2.5), the ladder (A2.6), G1′/G2/G3 (A2.7) and the plants (A2.8) are
unchanged in wording and digits. Plant (vii) completes on its A2 run.

## A3.3 What is read, and when

Nothing until G0‴ passes on all six sectors and plant (vii) fires; then A2.9 as written.
Cost: the χ = 1,024 rungs are priced by their rows; no price is predicted.
