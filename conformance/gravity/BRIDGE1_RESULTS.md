# BRIDGE-1 — adjudicated: the closure rung PASSES its freeze; four gates fire, four misfits extracted

*2026-08-27. Prereg b12ffdf + A1 d41a609 + A2 11baa64, instrument committed
separately, run log `bridge1_run.log`. Gates were staked SEPARABLE; the
verdict is per-rung.*

## PASSED under frozen gates, both triangulations
- **B4 — the closure rung, the campaign's headline**: the exact quantum
  parity followed the A1-derived sin²(kπ/4) law — ½, 1, ½, 0 — with
  closure defect against the staked memoryless bookkeeping δ = 0, ½, 0, ½.
  The classical geometric view closes at k=1, fails by COHERENCE at k=2,
  and fails again at the k=4 revival — exact rationals, staked before the
  instrument existed. **First frozen-gate pass of "classical geometry
  fails to close on quantum dynamics, measured."**
- **B3 — Gauss on every registry state** (the full-scope constraint gate
  that killed v2 now passes on genuine dynamics).
- **B7 — both plants FIRE** (wrong-side action; non-central pump — each
  convicted by the derivation that licenses its lawful twin).

## FIRED, each diagnosed by probe (the misfits)
- **M9 (B1 routes)**: the COMBINATORIAL route transcribed the flux phase
  as i¹ where A2 stakes i² — the checker was wrong, the evolution right;
  plus a dict-normalization artifact (explicit zeros) producing spurious
  route fires where distributions were equal.
- **M10 (B1 diff)**: "occupied ≠ empty at every k" was a DERIVATION ERROR,
  proved by state-level probe: the pump acts at k=1 (gauge sectors differ
  as states) but every staked marginal is provably blind to it, and at
  k=2 the sectors RECONVERGE EXACTLY (pump² = e meets the deterministic
  electric flip). The discriminator must be an interference observable.
- **M11 (B2)**: the pump SHIFTS the occupied branch's support, so the
  naive coherence is identically zero at odd k; at k=2 the supports
  realign (proved by probe) and the observable becomes posable.
- **M12 (B6 base)**: the support oracle was derived for pure gauge; the
  charged pair average halves the support (256), and the refined occ-only
  graph passing its 8^{V−1} oracle confirms the halving is the charge.
  The corrected oracle is an independent closed-form enumerator, not a
  hand formula (a first hand-derivation disagreed with itself — the
  dispute becomes a two-route gate in BRIDGE-2).

B5 (refinement) fired only by inheriting B1's fires; B4/B3 refinement
agreement held. No rescue of the fired rungs; BRIDGE-2 stakes the fixes.
