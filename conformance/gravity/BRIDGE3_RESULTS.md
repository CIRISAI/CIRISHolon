# BRIDGE-3 — adjudicated: ALL GATES PASS, BOTH PLANTS FIRE, both graphs

*2026-08-27. Prereg 8b12296 (frozen alone, pilot-designed, derivation-
staked), instrument committed after it, run log `bridge3_run.log`.
Exit 0 — the campaign's first fully green frozen run.*

## The geometry→matter rung, exact, both triangulations

The lawful channel (geometry-conditioned action on the gauge-neutral
occupation qubit) responded EXACTLY as staked at every step, with every
ratio confirmed by the independent occupation-labeled combinatorial route:

| k | C before → after | meaning |
|---|---|---|
| 1 | 0 → 0 | channel CLOSED: geometry has maximally recorded the matter (ω=0), no coherence exists to act on |
| 2 | C → **−C** | maximal response: geometry uniformly curved, the conditioned action lands in full |
| 3 | 0 → 0 | closed again |
| 4 | C → **C** | EXACTLY inert: geometry has coherently revived to flat and correctly has nothing to say |

Gauss held on every state including all conditioned images; refinement
verdicts identical; both plants fire. Spatial locality remains honestly
deferred (staked in the prereg: this Floquet family is parity-homogeneous
by symmetry; the symmetry-broken successor is the named rung).

## The program, closed at rung 0

With this run the reciprocal triple stands complete under frozen,
separable, machine-exact gates, each leg two-route verified on two
triangulations:

1. **Matter → geometry** (BRIDGE-2 B1′): ω = 0, 1, 0, 1 — matter's imprint
   on geometry is maximal at odd steps and coherently, completely erased
   at even steps.
2. **The closure rung** (B4, replicated three times): the classical
   geometric view fails to close through COHERENCE — δ = 0, ½, 0, ½
   against the best memoryless bookkeeping, failing exactly at the
   deterministic coherent flip and the revival.
3. **Geometry → matter** (BRIDGE-3 B2″): the back-reaction tracks the
   geometry's own content through its full cycle — silent, maximal,
   silent, inert.

The arc that produced it is part of the result: v1 VOID (its own
full-scope gate), v2 VOID (its own registry), BRIDGE-1 mixed (four fires,
four probed misfits), BRIDGE-2 five-of-six (one fire diagnosed by
theorem), BRIDGE-3 clean — five frozen campaigns, every fire diagnosed,
every fix derived before its instrument, plants firing at every
iteration. The fence, final and unchanged: a finite D4 toy — the smallest
exact instance of the bridge, with the SU(2) ladder named and waiting.
