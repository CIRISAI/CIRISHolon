# Pre-registration — CLOSURE-3: is the missing memory pairwise?

*Frozen 2026-08-28, committed ALONE. CLOSURE-2B's validated fire showed
the (Wilson, 't Hooft) one-body channel blind at every firing collision:
the memory closure lacks is not one-body. This campaign poses the next
order: the PAIR-correlator channel. Both outcomes are staked in advance
and both are findings — separation locates the memory at second order;
blindness pushes it toward whole-only.*

misfits: contacts M-GAUGE-UNIFORM-MOMENTUM (the pair channel is built
from gauge-invariant JOINT plaquette data, never per-edge marginals),
M-LOOP-BLIND and M-GAUGE-LAUNDER (all readings oriented), M-ONE-MODEL-DELTA
(collision form), M-PROBE-EIGENSTATE (carrier the zero-flux dressed
vacuum, named), M-PLANT-OBS and M-PLANT-SECTOR (plants below),
M-NULL-MISSTAKE, M-BARE-CHARGE, M-COND-PROBE (as CLOSURE-2B, unchanged),
M-ELECTRIC-BASIS and M-RING-MIXING (no new operator; WILSON-2B dynamics).

## Model and channel

CLOSURE-2B's model, trajectory, and collisions verbatim. The new channel:
- **v_pair** = v_conf + the 't Hooft triple + the exact JOINT weight
  9-vectors over PAIRS of plaquette holonomies, for all 10 plaquette
  pairs and the 5 (plaquette, rim-loop) pairs — every gauge-invariant
  second-order Wilson datum on the graph.

## Gates (all EXACT)

- **G0**: as CLOSURE-2B. witness: none
- **K1 — the collisions reproduce**: the same three firing v_conf
  collisions (1,5), (2,4), (5,7) recur (the trajectory is deterministic;
  this is a consistency gate on the instrument). witness: none
- **K2 — THE QUESTION** (EXACT, two-branch, both branches findings):
  branch (a): v_pair separates EVERY firing collision pair ⇒ the missing
  memory is second-order — classical pair data restores single-valuedness;
  branch (b): v_pair is blind at some firing pair ⇒ the memory is beyond
  second order on this model, pushed toward whole-only, recorded with the
  blind pairs named. witness: collision_refutes_memoryless
- **K3 — inheritance**: conservation through v_pair exact, as C3.
  witness: closed_view_inherits_conservation
- **B3**: as CLOSURE-2B. witness: none

## plants (carrier and sector per M-PLANT-SECTOR)

Each plant's carrier is asserted nonzero in the sector the plant acts on.
- **(i) pair-sensitivity**: two explicitly constructed states — the
  carrier plus a two-plaquette flux insertion vs the carrier plus the
  same insertions on separate branches of a superposition — whose ONE-BODY
  channels (v_conf and 't Hooft) are asserted EQUAL and whose pair channel
  must DIFFER. Carrier: the constructed pair; sector: the pair-channel
  difference, asserted nonzero.
- **(ii) mis-scale mutant**: as CLOSURE-2B's plant (ii), unchanged.
A missed plant VOIDs.

## Meaning

K2(a) ⇒ the coarse classical bookkeeping that closes this model is
(one-body + pair) — the memory is shallow, and requirement 3's channel
ladder terminates at second order here. K2(b) ⇒ the memory is
higher-order on even this smallest gauge model — the strongest available
toy evidence that what classical channels discard is whole-pattern
structure. Either way the ladder's next rung is determined by measurement,
not preference.
