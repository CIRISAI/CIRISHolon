# Pre-registration — CLOSURE-4: is the memory third-order?

*Frozen 2026-08-28, committed ALONE. The reopened memory ladder's next
rung. CLOSURE-3 (corrected) showed v_pair fires at (1,7): the memory is at
least third-order. This campaign poses the TRIPLE-correlator channel, with
the lesson mechanized: the refined view's OWN collisions are enumerated
from the start (M-FINAL-VIEW-COLLISIONS).*

misfits: contacts M-FINAL-VIEW-COLLISIONS (the gate structure exists
because of it), M-GAUGE-UNIFORM-MOMENTUM (all channel components are
gauge-invariant joint plaquette data), M-LOOP-BLIND and M-GAUGE-LAUNDER
(oriented readings), M-ONE-MODEL-DELTA (collision form), M-PROBE-EIGENSTATE
(carrier the zero-flux dressed vacuum, named), M-PLANT-OBS and
M-PLANT-SECTOR (plants below, carriers asserted nonzero in the sector each
plant acts on), M-NULL-MISSTAKE, M-BARE-CHARGE, M-COND-PROBE,
M-ELECTRIC-BASIS, M-RING-MIXING, M-STALE-INSTRUMENT (instrument with
results), M-NONBIJECTIVE-STEP and M-FIXED-POINT-TRAJECTORY and M-HOMOG and
M-KINEMATIC-NONLOCAL (not otherwise contacted — no new dynamics, no
locality claim).

## Model and channel

CLOSURE-3's model and trajectory verbatim. New channel:
**v_triple** = v_pair + the exact joint weight 27-vectors over all TRIPLES
of the six loop observables (5 plaquettes + rim) — every gauge-invariant
third-order Wilson datum on the graph.

## Gates (all EXACT)

- **G0**, **K1** (the three coarse firing collisions reproduce), **K3**
  (inheritance), **B3**: as CLOSURE-3's corrected instrument.
  witness: closed_view_inherits_conservation (K3); none (others)
- **Q2 — THE QUESTION** (EXACT, two-branch, both findings): enumerate
  v_triple's OWN collisions over the depth-8 trajectory. Branch (a): none
  firing ⇒ the missing memory is EXACTLY third-order on this model (pair
  fails, triple closes). Branch (b): a firing v_triple collision ⇒ the
  memory is beyond third order — pushed further toward whole-only, the
  blind pairs named. witness: collision_refutes_memoryless

## plants (carrier and sector per M-PLANT-SECTOR)

- **(i) triple-sensitivity**: two constructed states — correlated triple
  flux insertion vs its distributed superposition — with ALL one-body and
  pair components asserted EQUAL and the triple channel required to
  DIFFER. Carrier: the constructed pair; sector: the triple-channel
  difference, asserted nonzero.
- **(ii) mis-scale mutant**: as before. Carrier: the trajectory; sector:
  raw totals, nonzero.
A missed plant VOIDs.

## Meaning

Q2(a) ⇒ the coarse bookkeeping that closes this model is exactly
third-order — the memory ladder terminates one rung above phase-space.
Q2(b) ⇒ the memory survives third-order readings on the smallest gauge
model — the strongest toy evidence yet that the discarded structure is
whole-pattern. Either branch advances rung 5½ by measurement.
