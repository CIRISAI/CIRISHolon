# Pre-registration — FROB-ORIENT-1: orientation without laundering

*Frozen 2026-08-28, committed ALONE. Rung 5's next brick: the
non-ambivalent orientation leg. In D4 every class is inversion-closed, so
class readings launder loop orientation (M-GAUGE-LAUNDER, measured in
BRIDGE-1). In F21 = Z7 ⋊ Z3 (the smallest non-abelian NON-ambivalent
group) the two order-7 classes are EXCHANGED by inversion — orientation is
class-visible, and the D4-style ambivalent projection is provably blind.
This campaign measures both facts on exact states, which is what the
geometry sequence needs before the SU(2) chain.*

misfits: contacts M-GAUGE-LAUNDER (the campaign exists to discharge its
constructive half), M-LOOP-BLIND (the readings are full oriented class
weight vectors), M-PROBE-EIGENSTATE and M-FIXED-POINT-TRAJECTORY (carriers
are flux states, named, non-invariant by construction), M-PLANT-OBS and
M-PLANT-SECTOR (plants below), M-NONBIJECTIVE-STEP (no dynamics is staked
— this is a kinematic orientation campaign; the only maps used are gauge
projections, verified idempotent-up-to-scale), M-FINAL-VIEW-COLLISIONS
(no closure claim is staked), M-ONE-MODEL-DELTA and M-NULL-MISSTAKE and
M-BARE-CHARGE and M-COND-PROBE and M-ELECTRIC-BASIS and M-RING-MIXING and
M-GAUGE-UNIFORM-MOMENTUM and M-HOMOG and M-KINEMATIC-NONLOCAL and
M-STALE-INSTRUMENT (instrument committed with its results; none otherwise
contacted — no defect, model, channel-delta, electric-term, locality or
momentum claim appears here).

## Model

One triangular plaquette (3 edges, one cycle) over F21, 21³ = 9261
configurations, integer amplitudes. The group is built IN the instrument
from the presentation (a⁷ = b³ = 1, b a b⁻¹ = a²) with its multiplication
table verified associative by exhaustion before any gate. The two order-7
classes are C = {a, a², a⁴} and C⁻¹ = {a³, a⁵, a⁶} (inversion-exchanged:
non-ambivalence verified by exhaustion, as a gate).

## Gates (all EXACT)

- **N0 — group facts by exhaustion**: associativity, |F21| = 21, the class
  table, and NON-AMBIVALENCE (some class ≠ its inverse class) all verified
  in-instrument before anything else. witness: none (exhaustive check; a
  Lean brick is named, not claimed)
- **O1 — orientation is class-visible**: the Gauss-projected flux-C state
  and flux-C⁻¹ state have DIFFERENT oriented class-weight vectors over the
  plaquette holonomy. witness: none (measured gate)
- **O2 — the ambivalent projection is blind**: identifying each class with
  its inverse class (the reading D4's structure forces) makes the two
  states' vectors IDENTICAL — the laundering, reproduced deliberately on a
  group where it is avoidable. witness: none (measured gate)
- **O3 — gauge invariance**: both carriers Gauss-held. witness: none

## plants (carrier and sector per M-PLANT-SECTOR)

Each plant's carrier is asserted nonzero in the sector the plant acts on.
- **(i) symmetrized control**: the state built from C ∪ C⁻¹ flux must read
  IDENTICALLY under both the oriented and ambivalent readings — a state
  with no orientation content must show none (convicts the oriented
  reading against false positives). Carrier: the symmetrized state;
  sector: its class-weight support, nonzero.
- **(ii) wrong-side action**: a left-action-only "gauge" transform must
  break the Gauss check. Carrier: the flux-C state; sector: total.
A missed plant VOIDs.

## Meaning

O1+O2 ⇒ "orientation is physical and class-readable exactly when the
group is non-ambivalent; D4's blindness was the group's, not the
observable's" — the geometry sequence's orientation leg, earned. Successor:
PT-2T (the deficit ladder), then the SU(2) subgroup chain.
