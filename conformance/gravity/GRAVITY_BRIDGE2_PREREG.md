# Pre-registration — GRAVITY-BRIDGE-2

*Frozen 2026-08-27, committed ALONE before any instrument. Carries misfits
M1–M12. Model, dynamics, B3 (Gauss full-scope), B4 (closure, already
passed and re-run unchanged as a replication rung), and the two plants are
BYTE-IDENTICAL to BRIDGE-1 + A1/A2. Only the four fired rungs are
re-staked, each fix a derivation:*

- **B1' (matter→geometry, interference discriminator — fixes M9/M10):**
  the reading is the exact normalized gauge-sector overlap
  ω(k) := |⟨Ψ_empty(k)|Ψ_occ(k)⟩|² / (norms), computed BOTH by the
  state-vector evolution and by the corrected combinatorial route (flux
  phase i^{κ}, κ(r²)=2; distributions compared after dropping explicit
  zeros). STAKED from the probed algebra: ω(2) = 1 and ω(4) = 1 exactly
  (pump² = e meets the deterministic electric flip — sector reconvergence
  proved by probe in the BRIDGE-1 record), ω(1) < 1 and ω(3) < 1 strictly
  (the pump acts; the probe exhibited differing gauge sectors at k=1).
  Routes must agree exactly at every k ≤ 4.
- **B2' (geometry→matter at k=2 — fixes M11):** run the superposed-
  occupation state to k=2 (supports realigned, proved), then apply
  U_geo→mat conditioned on p0; the exact occupation coherence must CHANGE;
  the same conditioned on a distant flat plaquette's boundary must be
  EXACTLY inert. (Same observable as BRIDGE-1's B2, at the k where it is
  posable.)
- **B6' (oracles — fixes M12):** two-route support gate: an independent
  closed-form ENUMERATOR over (g_c, g_1, g_2, g_3) with the charged weight
  (ρ(g₁)ρ(g₂)ᵀ)₀₀ + the |11⟩-seed partner term must reproduce (a) 512 on
  the uncharged model (calibration rung — it must match the proven
  8^{V−1}), and (b) the evolved charged vacuum's measured comp-0 support.
  A first hand-derivation disagreed with the instrument (128 vs 256); the
  enumerator-vs-evolution agreement IS the gate, and whichever route is
  wrong will be convicted by the calibration rung.
- **B5' (refinement):** B1', B4 verdicts identical on the refined
  (occupation-only) graph.

Meaning, kills, VOID semantics, fence: unchanged from BRIDGE-1. A missed
plant voids; no post-run substitution; the results cite this commit.
