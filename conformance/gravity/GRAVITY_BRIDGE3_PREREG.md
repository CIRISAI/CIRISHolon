# Pre-registration — GRAVITY-BRIDGE-3: the geometry→matter rung

*Frozen 2026-08-27, committed ALONE before the instrument. Model, dynamics,
B3, and plants byte-identical to BRIDGE-1/2. ONE re-staked rung (B2″,
fixing M13), designed from a LABELED exploratory pilot (recorded in the
BRIDGE-2 adjudication trail) and staked from derivations:*

**The operator** (lawful by construction): G_p := P_{parity1}(p) ⊗ σ_z^occ
+ P_{parity0}(p) ⊗ I — a geometry-conditioned action on the OCCUPATION
qubit, the gauge-neutral matter degree of freedom. Gauge-invariant (the
projector is class-diagonal; σ_z^occ commutes with every gauge action);
NOT occupation-blind (M13's theorem does not apply); Gauss-preserving
(verified in the pilot, gated again here).

**The staked readings**, on the occupation-superposed state evolved k
steps, reading the exact unnormalized occupation coherence C (ratios are
exact rationals; the state's norms cancel):
- k = 1 and k = 3: **C = 0 exactly** — B1′'s measured orthogonality
  (ω = 0) closes the channel: geometry maximally distinguishes the
  branches, so no coherence exists to act on.
- k = 2: **G_{p0} flips C exactly** (C → −C): every support configuration
  is parity-1 (the deterministic electric flip, B4's own k=2 row), the
  projector is the identity there, and σ_z^occ acts in full.
- k = 4: **G_{p0} exactly INERT** (C → C): the revival returns every
  plaquette to parity-0, the projector annihilates, the operator is the
  identity — geometry has nothing to say and correctly says nothing.
- B3: Gauss holds on every state including all G-images.
- Two-route: an occupation-labeled extension of the combinatorial route
  must reproduce every staked ratio exactly.
- Refinement: identical verdicts on the refined (occupation-only) graph.
- Plants: unchanged pair, must both fire; missed plant voids.

**Honest scope, staked now:** SPATIAL locality cannot be posed in this
Floquet family — the per-edge electric step makes every plaquette's parity
content identical (the pilot measured the p0- and distant-conditioned
responses equal for exactly this reason), so the control here is
STATE-locality: the response tracks the geometry's actual content through
its full oscillation (flip / closed / flip / inert). A spatial-locality
control requires breaking the toggle symmetry (spokes-only electric term,
or a larger graph) and is the named successor rung, not smuggled in here.
