# Pre-registration — LOCAL-1: locality as a response function, with an exact light cone

*Frozen 2026-08-28, committed ALONE. The reformulation M-KINEMATIC-NONLOCAL
demands: BRIDGE-6/7B staked locality on CONDITIONING, and the Gauss
projector correlates every region kinematically, so both stakes died for a
reason that was in the constraint, not the dynamics. Here locality is a
RESPONSE FUNCTION: evolve the SAME state with and without one local unitary
perturbation; constraint correlation is present in both arms and cancels
EXACTLY; what survives the difference is dynamical propagation and nothing
else.*

misfits: contacts M-KINEMATIC-NONLOCAL (the campaign exists to discharge
it), M-ELECTRIC-BASIS (WILSON-1's convicted Fourier kernel is REUSED as
plant (ii) — the registered defect becomes the conviction instrument),
M-NULL-MISSTAKE (every zero staked below is a per-arm difference the
dynamics actually constrains, not a cross-arm equality it does not),
M-PLANT-OBS and M-PLANT-SECTOR (plants state carrier and sector),
M-BARE-CHARGE (dressed pair carried, G0 unchanged), M-COND-PROBE (the full
endogenous T is used, pump included), M-HOMOG and M-LOOP-BLIND and
M-GAUGE-LAUNDER (observable is WILSON-2B's oriented triple, per plaquette),
M-PARITY-PROTECT (Z3, no parity sector; noted).

## Model

WILSON-2B's validated machinery (Z3, Eisenstein-exact, dressed pair, the
covariant electric term, the full endogenous T) on the TAILED graph: the
fan disk PLUS a pendant triangle [(3,d1), (d1,4), reusing rim edge (4,3)] —
12 edges. **U_E acts on EVERY edge** (spokes, rim, pendant), so the whole
graph is dynamical and the plaquette-adjacency chain from the perturbed
edge to the pendant has length 3: e* ∈ {P4, P0} → P3 (shares a spoke with
P4) → pendant (shares rim edge (3,4) with P3).
- invariance: U_E is the shift polynomial (1+ωL₁+ωL₂)/√3 per edge —
  gauge-covariant by construction (M-ELECTRIC-BASIS discharged in the
  freeze); U_B and K_charge as in WILSON-2B.

## The response function

`R(p, k)` := the oriented weight triple of plaquette p at step k under
`T^k · L₁(e*) · |ψ₀⟩` MINUS the triple under `T^k · |ψ₀⟩`, where L₁(e*) is
the unitary flux shift on spoke e* and |ψ₀⟩ the dressed vacuum. Both arms
are unitary in-ring and scale identically, so "zero response" is EXACT
integer equality of triples, arm against arm, step by step.

## Gates

- **G0** (EXACT): dressed sector nonzero, Gauss held; checked first.
  witness: none (instrument-checked)
- **L1 — THE LIGHT CONE** (EXACT): `R(pendant, 1) = 0` and
  `R(pendant, 2) = 0` — identically, as integer triples. This is the
  locality claim: a perturbation cannot outrun plaquette adjacency.
  witness: none (measured gate; the Lieb–Robinson-style bound for this
  Floquet is named as a Lean follow-up, not claimed)
- **L2 — propagation** (EXACT): `R(P4, 1) ≠ 0` — the perturbation is not
  invisible; the near plaquette responds at once. (If L2 fails the
  campaign VOIDs — a response function that never responds poses nothing.)
  witness: none (measured gate)
- **L3 — arrival** (EXACT, two-branch): `R(pendant, 3)` or `R(pendant, 4)`
  ≠ 0 → "the signal arrives at the adjacency speed"; both = 0 → recorded
  as NO-ARRIVAL (dispersion too weak at this size), which is NOT a fire —
  the cone (L1), not the arrival, is the locality claim.
  witness: none (measured, two-branch, both branches stated)
- **B3** (EXACT): joint Gauss on every registry state, both arms.
  witness: none (measured gate)

## plants (carrier and sector per M-PLANT-SECTOR)

Each plant's carrier is asserted nonzero in the sector the plant acts on —
for plant (i), the sector of the effect: the pendant response itself.
- **(i) direct-hit control**: perturb a PENDANT edge instead of e*; then
  `R(pendant, 1) ≠ 0` must hold — convicting the response machinery's
  sensitivity at the very plaquette whose zero L1 stakes. Carrier: the
  dressed vacuum; sector: the pendant-response support, asserted nonzero
  when the perturbation is adjacent.
- **(ii) the convicted kernel**: swap U_E for WILSON-1's Fourier kernel on
  one edge; B3 must fire within 1 step (it did, historically, on every
  state — the registry defect reused as the conviction). Carrier: the
  dressed vacuum; sector: total, nonzero.
A missed plant VOIDs; an unposable plant VOIDs.

## Meaning

All gates → "in an exact constrained gauge theory with fully endogenous
dynamics, DYNAMICAL influence is local — bounded by an exact light cone —
even though KINEMATIC constraint correlation is everywhere; the two are
separated by the response function, and the earlier locality deaths are
explained, not contradicted." L1 fires → dynamical influence outruns
adjacency in this model and the locality programme has a real crisis, kept
marked. Downstream (named, not claimed): LOCAL-2 composes this cone with
the reciprocal pump in one model; SU(2)-via-2T inherits the response-
function formulation for the deficit angle.
