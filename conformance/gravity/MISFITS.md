# The misfit registry — every named defect the bridge campaigns have produced

*The prereg audit refuses a freeze whose text contacts a registered misfit's
keywords without citing it. That is the mechanism that turns "we knew this"
into "we applied this": the registry is grep-armed, not advisory.*

| id | one line | born in |
|---|---|---|
| **M-GAUGE-LAUNDER** | a conjugacy-class observable cannot see orientation; ambivalent classes launder the sign | BRIDGE-1 |
| **M-PARITY-PROTECT** | the parity sector is dynamically protected; observables inside it read constants | BRIDGE-1 |
| **M-LOOP-BLIND** | every staked loop-class marginal was blind to the branch difference; "curvature at a distance" needs a separated gauge-invariant observable, not a global state distinction | BRIDGE-1 (M10) |
| **M-PLANT-OBS** | observability is instrument-relative: a plant must be re-derived for THIS instrument and pre-checked to fire — four occurrences (BRIDGE0-V2, BRIDGE-5, BRIDGE-6 twice) | BRIDGE0-V2 (M8) |
| **M-PLANT-SECTOR** | a plant's carrier must be nonzero IN THE SECTOR THE PLANT ACTS ON, not merely nonzero overall | BRIDGE-6 |
| **M-BARE-CHARGE** | a bare charged pair is diagonal-invariant only; Gauss acts per vertex and annihilates it — dress with a Wilson line and VERIFY invariance before use | BRIDGE-5 (M14) |
| **M-HOMOG** | the fan-disk Floquet family is parity-homogeneous and one term substitution does not break it; spatial locality needs a structurally inhomogeneous graph | BRIDGE-3, confirmed BRIDGE-6 |
| **M-VOLUME-SCALE** | a lattice grid's N must scale with the coupling (N ≳ 20√x for the Schwinger family, per Bañuls et al. in our own sweep); one N range for every x under-resolves the fine columns | SCHWINGER-2 |
| **M-COND-PROBE** | an operator applied AFTER the step is a conditioned probe, not endogenous dynamics; reciprocity claims need the operator inside T | BRIDGE-3 (external review) |
| **M-ONE-MODEL-DELTA** | a defect measured against one chosen model earns "worse than that model"; the collision/minimax form earns "best memoryless" | BRIDGE-2 (external review) |
| **M-KINEMATIC-NONLOCAL** | the Gauss projector correlates every region; conditioning on ANY plaquette of a constrained state touches matter — a locality stake must separate dynamical propagation from kinematic constraint correlation | BRIDGE-7B |
| **M-ELECTRIC-BASIS** | an electric term must be a FUNCTION OF SHIFT OPERATORS; a basis-change (Fourier) kernel is not gauge-covariant and B3 fires at once — the freeze must carry an invariance argument per operator | WILSON-1 |
| **M-NULL-MISSTAKE** | a conservation null must be staked on the quantity the law constrains (per-carrier invariance across steps), not on a quantity it never constrained (equality between different carriers) | WILSON-2 |
| **M-PROBE-EIGENSTATE** | a response probe's carrier must not be an eigenstate of the perturbation (the uniform electric vacuum is shift-invariant, so R ≡ 0 vacuously); the freeze must name WHICH vacuum | LOCAL-1B |
| **M-RING-MIXING** | at scale √3 every Z[ω] circulant unitary is permutation-or-maximal-mixing, so a propagation probe thermalizes in one step; weak couplers first exist at scale 3√3 — check the ring scale before staking dynamics | LOCAL-1C |
| **M-GAUGE-UNIFORM-MOMENTUM** | gauge invariance forces every single-edge momentum marginal to be exactly uniform (the Gauss action translates the edge variable), so a phase-space channel's momentum half must be built from invariant correlators — the dual ('t Hooft) loop is the natural one | CLOSURE-2 |
| **M-STALE-INSTRUMENT** | a results document without its instrument's commit is not banked: three verdicts in one session were claimed on main while the runnable files were working-tree-only; the CI reproducibility gate now imports every instrument and fresh-runs the fast ones. WIDENED 2026-09-01, the inverted variant: a COMMITTED instrument citing a path keyed to the session that wrote it passes the working-tree check and resolves from inside that session, then dies with it — the citation fails only when followed from OUTSIDE the writing session, so the enforcement is exactly that (follow every scratch citation from outside, credit bigqvm-demo/saturation3-mesh). Five committed files carried the defect: two fixed by their owner at 625cc97, four re-pathed by the lead (all were WRITE destinations, so no content was lost — but the rung5 spectra dump was already gone, results surviving only as transcription). A half-fix is its own instance: an env override whose DEFAULT is the session path dies just as dead. CI gate 10a3 now greps executable conformance files for session-keyed paths. SECOND VARIANT, 2026-09-01, which that gate does NOT reach and neither does the working-tree check: SATURATION-3's G2 instruments — the source of the 91.0% bitwise divergence founding [[M-DEVICE-CLASS]] — were cited from a PROSE RECORD as `scratchpad/s3gpu/...`, a plausible RELATIVE path that reads as the repository's scratch directory and named a location the files had never been in; they lived in a per-session scratchpad and were one cleanup from taking the result's reproducibility with them. **Invisible on BOTH of the gate's axes: it is prose (deliberately out of scope, and rightly so) and it contains NO session-keyed string to grep — the path is relative, so `claude-1000` matches nothing.** The working-tree-only case is detectable because the file is there and shows as untracked; this one greps as a CLEAN TYPO, a path that simply does not exist, which reads as a misspelling rather than as a lost instrument — and from inside the writing session it resolves perfectly. Enforcement is therefore a HUMAN rule, not a grep: FOLLOW EVERY SCRATCH CITATION FROM OUTSIDE THE SESSION THAT WROTE IT. A BLANKET GATE WAS BUILT, MEASURED AND REJECTED rather than left as a suggestion: resolving every backticked path-shaped citation in the conformance records gives 167 unresolved of 594 scanned, almost all legitimate cross-repo prose references (`Core/Third.lean`, `HCl.json`), so a refusal would be switched off within a day; and the narrow `scratchpad/` subclass has exactly THREE instances tree-wide, ALL of which resolve against the sibling CIRISOntology root — the class is clean and the checker's own first verdict on them was a false positive from checking one repo root. Repair that beats annotation: the G2 instruments were rescued and RE-RUN, returning the correctness figures identical to the digit, converting a dead citation into a re-run result. THIRD VARIANT, 2026-09-01, at file level rather than session level: **a citation to a SHARED WORKING TREE is a citation to a MOVING OBJECT.** This lane cited a neighbouring lane's `ooo_ecore.log` as independent corroboration of a timing anomaly; it was their uncommitted pre-fix run, they overwrote it minutes later, and their committed logs say the opposite. The citation resolved when it was read and resolves differently now, and **nothing in the reference said which state it named** — so the claim could not be checked by anyone, including its author. Enforcement: *cite a commit, or say explicitly that you are citing uncommitted state and pin what you read* (hash the bytes, quote the lines). Note the family resemblance across all three variants: a session path is correct only inside its session, a working-tree path is correct only at its instant, and both are invisible from where they were written. Credit gpu-prod for the general form; the founding case is this lane's error against their file | external re-review 2026-08-28; widened via fleet sweep 2026-09-01; prose/relative variant found, rescued and blanket-gate route measured-and-rejected by saturation3-mesh 2026-09-01 THIRD VARIANT, 2026-09-01, the baseline wearing a control's clothes: a committed ARTIFACT used as the "before" of a comparison is a stale instrument the moment anything between its generation and the reading can move the measured quantity. mixtures-engine compared shipped `HCl.json` (emitted 08-30 19:05) against a live-tree regeneration (09-01) and found 186/192 energy knots moved, max 3.979e-12 Ha with the grid 0/192 different — a sound measurement — then attributed it to a lane's uncommitted solver rewrite that the lead had NAMED AS THE THING TO CHECK hours earlier. The interval held that work AND a day of commits: **a control that predates two variables attributes to neither**, and a pre-announced suspect is a prior, not a discriminator — naming it in advance is what made the two-variable comparison feel like a one-variable one. The tell is available before any compute: the two endpoints of the comparison differ by an INTERVAL rather than by a CHANGE. The discriminator is parent-versus-commit on the same tree (the accused landing proved bit-identical to its own parent; the real cause, `4884704`, does not), and the enforcement is to run it BEFORE writing the attribution, not after. Occasion worth its own note: the cause was an unmentioned optimisation to the FCI sigma kernel that walks a sparse touched set in first-touch order instead of every index ascending — it changes only FP SUMMATION ORDER, so it moves every pinned bit-digest downstream while moving no physics, and its commit message is titled after an ozone scan. Curves too small to reorder (H2, HHe, He2) did not move, which is the only reason B1's bit-identity reference survived — out of the blast radius, not lucky. CASE LAW from the same interval, and the reason this rung is worth its length: **an identity gate's error message names the last thing its author imagined changing, not the cause.** One unannounced kernel optimisation (4884704) put three lanes' bit-identity gates red at once, and each accused a different innocent party — W1's mask widening ('was supposed to change which spaces can be ADDRESSED and nothing about what they evaluate to'), 'the artifact and the code have diverged; regenerate it', and my own shipped uncertainty — because every one of them compares an artifact banked before the boundary against a build after it. RULED (2026-09-01): the regime is ACCEPTED, not reverted; the boundary is named in tier.rs at the touched_kl walk; artifacts re-bank per lane but every re-banking commit carries ONE identical cause line, since what kills the lane-blames-itself failure is the shared cause and not atomicity. Two riders learned the hard way: a FROZEN CONTROL is retired-as-discharged and never re-banked (re-banking W1's pre-widening baseline would destroy the thing it exists to be), and there is NO size threshold for the reach — right mechanism, wrong axis: what governs is whether the sparsity pattern's first-touch order differs from ascending, per system (Cl 9 dets moved, Br 18 did not, I 27 moved). |
| **M-FIXED-POINT-TRAJECTORY** | a trajectory-based closure gate is vacuous on a carrier that is a fixed point of the dynamics (the uniform-over-sector state is invariant under any sector-preserving permutation); stake closure UNIVERSALLY (well-definedness of the descended map over ALL configurations) or on a provably moving carrier | external re-review of EINSTEIN-ADM-1 |
| **M-FINAL-VIEW-COLLISIONS** | "the refined view restores closure" requires enumerating the REFINED view's OWN collisions, not only its separation of the coarse view's — CLOSURE-3 claimed second-order memory while v_pair itself fired at (1,7) | external re-review, confirmed |
| **M-NONBIJECTIVE-STEP** | any map called dynamics or unitary must be verified BIJECTIVE, and constraint preservation must be checked on the stepped states of EVERY chart — the refined torus step collapsed the edge splitting and broke refined Gauss | external re-review, confirmed |
| **M-TAG-AS-PROPERTY** | a verdict computed from construction metadata (a builder tag, a name, a size) is a lookup wearing a measurement's clothes — SELECTOR-5's C4 "Schur/projective obstruction" was reproduced exactly, on all 430 groups, by (tag, name, \|G\|), and zero of its survivors owed it to a property computed from the group; enforce with a tag-shuffle plant: relabel everything, no verdict may move | SELECTOR-5 refutation, 2026-08-30 |
| **M-BASE-RATE-OMITTED** | precision quoted without the population's base rate is an enrichment claim with its denominator deleted — "97.8% of survivors are SM subgroups" over a population that is 94.4% SM subgroups buys 3.4 points, and the sweep's own script printed the base rate its results file omitted; every selection claim carries the eligible-pool rate and a permutation null beside it | SELECTOR-5 refutation, 2026-08-30 |
| **M-PRESENTATION-VERDICT** | a criterion on mathematical objects must be invariant under isomorphism, demonstrated on re-presented instances — SELECTOR-5's gauntlet passed A₄ and failed Δ(12), which are the same group; the permanent regression is a staked list of isomorphic presentation pairs receiving bit-identical verdicts | SELECTOR-5 refutation, 2026-08-30 |
| **M-POPULATION-CHOICE** | a hand-chosen census read as "the landscape" makes the population a silent free parameter — SELECTOR-5's 430 covered under 15% of the true \|G\| ≤ 128 landscape, and adding the metacyclic groups its own families generalize dropped precision from 0.978 to 0.926 with every new survivor on the hard-coded order list; declare the population rule, gate completeness against an external count (A000001), and name every exclusion | SELECTOR-5 refutation, 2026-08-30 |
| **M-CONJUNCTION-MONOTONE** | the passing fraction of a nested conjunction of filters is non-increasing for ANY predicates whatsoever — quoting that monotonicity as "systematic thinning proving selection" states a theorem about conjunctions as if it were a finding about the world; thinning is evidence only against a null over filter choices | SELECTOR-5 refutation, 2026-08-30 |
| **M-SORTS-NOT-SEPARATES** | a quality bar can RANK rather than separate: before reading a threshold as converged/unconverged, check whether ANY member of the class clears the underlying target (no multi-determinant heavy atom reached Davidson's 1e-11; the 1e-10 bar sorted gave-up solves with the published/refused line at a factor of 1.1) — and check whether the bar sits in the numerical neighbourhood of an instrument threshold governing a DIFFERENT quantity | ELEMENTS-3 diagnostic, 2026-08-30 |
| **M-EXIT-DISCRIMINATOR** | a solve record keeping only the residual makes iteration-cap and subspace-stagnation indistinguishable, and they are different facts (stagnation means more budget buys nothing); every terminating instrument records WHY it stopped, and the discriminator field must be READ, not merely carried (davidson_iters sat unprinted one field from a repaired one) | ELEMENTS-3 diagnostic, 2026-08-30 |
| **M-MAX-OVER-SUCCESSES** | a maximum taken over a set that contains a failure is not a bound on that set: defining a <= admission door by the largest success admits every failure below it; a sufficient-condition door takes the wall minus one, and best_reached vs first_wall are different numbers an instrument must not conflate | MPS ladder review, 2026-08-30 |
| **M-BUDGET-LAUNDER** | budget exhaustion must VOID loudly and may never fall back to a scorable verdict, because compute expense is a structural property of the subject and can correlate with the label — a gate that scores exhausted cases is measuring the budget and calling it physics; report VOID structure so a correlated refusal pattern is visible | SELECTOR-6 design v2, caught pre-run, 2026-08-30 |
| **M-UNTESTED-GAP** | before staking a prediction, plot the existing points on the hypothesised axis and check the hypothesis predicts anything at the staked point — interpolating across a hole in your own data, with a confident feeling imported from a DIFFERENT axis, is a stake that predicts nothing (S2's 23,409 dets sat between all successes at <=225 and the one failure at 44,100); reach claims must match EVERY axis of the target (orbital count, filling, topology) | elements3 stake failure, self-reported, 2026-08-30 |
| **M-FOREIGN-DOMAIN-CORROBORATION** | a passing result in a different domain read as evidence about yours: name what code a result EXERCISES before crediting it across a boundary (a pure-Python census "validating" a Rust engine's masks; 5-orbital toy spaces as "transition metal DMRG"); a same-commit port is the defect mirrored, not verified, and a swept commit is not a second source | four instances incl. the lead, 2026-08-30 |
| **M-IMPORT-EXECUTES** | importing an instrument executes its module scope — a module-level open-for-write TRUNCATED a predecessor's committed run log on first import; prior records are reachable only through proof-carrying doors (pinned-blob extraction that verifies its neutralising diff and asserts the record's bytes after import) | SELECTOR-6 design, 2026-08-30 |
| **M-VACUOUS-SUCCESS** | a verifier must assert its WORK COUNT, not only its failures: zero checks under an "ALL CHECKS PASSED" banner (wrong invocation directory), a conservation gate passing on a scene whose atoms never moved, a guard on an export that never existed — success reported for work not done, differing only by where it ran | three instances, 2026-08-30 |
| **M-CACHE-KIND** | record kinds sharing one key namespace let existence stand in for certification: a completion counter must count the CERTIFIED shape, kind belongs IN the cache key, and a shape mismatch on read must RAISE (returning stale-recompute overwrites the other kind) — "42/42 done" was a counter over the wrong predicate | ELEMENTS-1 cache repair, 2026-08-30 |
| **M-DEVICE-CLASS** | a faster arithmetic route that changes BITS makes the artifact device-class-dependent, and a determinism gate proven within one device class does not compose across classes: SATURATION-3's GPU sigma agrees with the CPU's to 3.033e-15 relative and differs BITWISE on 188,363 of 207,025 entries (91.0%) — both answers correct, not the same artifact — so a table built on one device is not the table built on the other, mixed-device generation cannot satisfy a bit-identity gate, and "prefer the faster device above a measured size crossover" makes the trailing bits of every result a function of load, driver and library version; declare the device class as part of the ARTIFACT and let dispatch choose only within it. Convergent precedent: two fp32 runtimes disagree as much as 4-bit quantisation does — compare only within one runtime | SATURATION-3 G2, 2026-08-30 |
| **M-IDLE-CALIBRATED-TIMEOUT** | **a sensor not calibrated in the REGIME IT RUNS IN fails silently and optimistically**, and that is what makes it dangerous rather than merely wrong — it does not throw, it blesses. COLLAPSED 2026-09-01 to ONE face with TWO DIRECTIONS, on bigqvm's recommendation and my ruling as the id's owner: the earlier WRONG DURATION / WRONG EXPOSURE split was an axis that was never there, and three successive mechanisms were proposed for it and all three died. There is one law and the direction is free. FORWARD — a constant measured IDLE and applied LOADED reaps healthy work (this lane's reaper). REVERSE — a baseline measured LOADED and applied to DETECT load certifies everything (bigqvm's quiet-window gate). Same defect mirrored, and both fail OPTIMISTICALLY, which is the sentence that carries the entry. RESOURCE-1's reaper is the first: polling a real 72-node generation at loadavg 40+ it measured its holders' true step at 402–702 polls (2–3.5 s) per H3 node against the ~8 ms an idle box gives — a factor of ~400 — and a flat grace sized from idle timings convicted 1115 LIVE workers (0 after the grace was made self-calibrating). bigqvm's quiet-window gate is the second: its 55 ms calibration job read 0.055 s against a 0.052 s record AT LOADAVG 46 — 1.06×, inside any sane gate — and so certified a saturated box as quiet, NOT because short jobs get an idle core — FALSIFIED on evidence that carries NO BASELINE: a 26.7 ms job burned 100–121 ms of CPU per fold in ALL THREE core placements against a quiet ceiling of ≤26.7 ms (descheduling consumes no CPU, so it ran slowly ON-CORE rather than waiting), and the 55 ms probe's CPU time tracks its wall, so neither was ever descheduled — but because of its BASELINE: the 0.052 s record was itself measured UNDER LOAD, so the gate compared LOADED TO LOADED and passed everything. Against true quiet baselines the two probes are equally sensitive (4.30× and 4.22×), so bigqvm's working-set explanation and the 3.5× ratio are both WITHDRAWN (`conformance/bigqvm/run_when_quiet.sh`, whose header keeps the rejected d=45 gate as the transferable failure). Note the symmetry of the harm: an idle-calibrated timeout REAPS HEALTHY WORK, a short probe BLESSES A LOADED BOX — opposite errors, same silent optimism. The fix is structural in both cases and never a bigger constant: learn the constant from the holder's own completed work in the environment where it runs, and — THE REMEDY THAT COVERS EVERY CASE THIS ENTRY HAS COLLECTED, where none of the three mechanism stories covered even two — **TAKE THE BASELINE IN THE REGIME YOU WILL COMPARE AGAINST, OR STATE WHICH REGIME IT CAME FROM. A RECORD WITHOUT ITS CONDITIONS IS NOT A RECORD.** Joint bigqvm/mesher; ADOPTED FLEET-WIDE by the lead as a launch-header requirement (loadavg at start AND end, core class, and clock as a fraction of advertised — mesher's, whose clock moved 54% to 52% inside one 30-second run, invisible in any loadavg), so a future ratio against a stored record can check the record's regime before believing itself — and DECLARE THE CORE CLASS, because P-vs-E is [[M-DEVICE-CLASS]]'s obligation one level down: a citable ratio is a function of core class, so an undeclared placement is an undeclared variable, and refuse to judge a holder still in WARMUP that has not completed a step. Temporal precedent: the hardware campaign's calibrations-are-rented lesson (a band frozen from a prior epoch decayed 88×). NEIGHBOUR, distinct and bigqvm's to name if they want an id: a SINGLE-SAMPLE sensor cannot see a REGIME CHANGE — loadavg read only at a sweep's start misses started-quiet-finished-loaded — which shares the silent-optimistic failure but has a different fix (sample both ends and refuse the label when they disagree, as their sweep now does) rather than a different calibration THE DEAD MECHANISMS, KEPT VISIBLE because the entry's own history is its best argument for the remedy: THREE successive explanations were offered for a probe-side effect that DOES NOT EXIST — (i) 'a short job gets an idle core', (ii) 'wrong duration', (iii) 'wrong exposure / working set' — and the founding datum they explained (a 1.06x-vs-3.5x blindness ratio) was itself the RATIO OF TWO WRONG BASELINES, 2.37/1.73 = 1.37. Against true quiet baselines both probes are equally sensitive (4.30x vs 4.22x). The lesson is not that the mechanisms were bad guesses; it is that no mechanism story could have been checked without a baseline in a known regime, so the effort went into explaining a number instead of qualifying it. THE THROUGH-LINE, which is the law in plain words — mesher: 'I keep reporting the shape of my reasoning as though it were the shape of my data'; bigqvm: 'I kept reporting the shape of my baseline as though it were the shape of the world.' PROVENANCE NOTE worth more than any single defect here: across two lanes this entry took NINE mutual retractions and NOT ONE INSTRUMENT FOUND ITS OWN ERROR — each found the other's, and the one self-catch (mesher's third) came only after they applied a remedy the other lane had asked for. | RESOURCE-1 live reaper + bigqvm quiet-window gate, 2026-08-30; probe-side mechanism corrected by mesher's counter-evidence 2026-08-30, then WITHDRAWN ENTIRELY with its founding datum by bigqvm 2026-09-01 (7a70256/a15ce08); falsified content corrected in place by its author under the lead's ruling (bc62309), faces collapsed and remedy unified by the id's owner (this lane) |
| **M-PROBE-THE-RESOURCE** | a health probe that interrogates the HOLDER ("is the process scheduling") instead of the RESOURCE ("can I actually write") passes exactly while every real operation fails: during the 2026-08-30 disk-full window every writer on the box was scheduling, burning CPU, and unable to write a byte — healthy by any process probe, dead by the only probe that matters. Probe the operation the lease exists to guarantee, at the resource; and because a resource probe can pass at lease time and be false milliseconds later, a lease's meaning must carry its own horizon — the probe buys validity NOW, every USE is itself a probe whose failure is authoritative, and only the ledger entry is guaranteed beyond the lease. Corollary rung for reapers: "is anything ELSE on this machine progressing?" — a timeout reaper in that window would have killed the whole machine's work, correctly reading the symptom and misdiagnosing the cause | RESOURCE-1 design, disk-full case study, 2026-08-30 |
| **M-MAINTENANCE-LENS** | a defect metric computed on a lens that HIDES the variable the repair acts on is constructively incapable of measuring repair efficacy — the collapse it reports needs the repair COMMAND issued, not the repair's restorative content. Verified on Six Birds' packaging exhibit (their own p_repair knob at 1.0/0.5/0.0 gives an identical Def(E)=0 at τ=2; lens (y,r,φ) discards u, the repaired variable; REPAIR moves nothing the lens sees), mechanism confirmed from source at cf0ec753. Own precedent, same family: the gain-plateau finding — a repair holds SIZE exactly forever while DIRECTION decays to floor, so the metric must read what the repair is FOR (fidelity), not what the lens happens to carry. CORROBORATION, and it is the strongest kind: this programme found the same shape INDEPENDENTLY and BEFORE the two met — `maintenance_sweep.py:725`'s ARM-MISMATCH ran full upkeep (q=1) pointed at the ORIGINAL support after drifting the structure, and P11 was FALSIFIED: the mis-targeted repair still held the share in full (E8: 2.773 of 2.773) against 0.000 unmaintained (`MAINTENANCE_SWEEP_RESULTS.md:336`). A misfit whose shape is found twice before the programmes meet is as validated as a misfit gets. Standing obligation: every rent/repair claim of ours re-audited against this — does the instrument's lens contain the repaired variable, and has a known-null repair (issue-the-command-restore-nothing) been run as the control? Re-audit discharged for CIRISHolon 2026-08-30 (`conformance/gravity/MAINTENANCE_LENS_REAUDIT.md`): of five families ONE passed cleanly on first look, and the expectation recorded in the proposal had been that most would | Six Birds cross-run + maintained-holonomy, 2026-08-30 |
| **M-PROVENANCE-OVERREACH** | a pin names what it measured, never what you inferred alongside it: a launch header printed the binary's true sha256 BESIDE "repo HEAD = 4e6f85d", but the build immediately before had FAILED (a sibling mid-edit on a new workspace member), so the bytes were an earlier build and the HEAD line asserted a relationship nothing verified — the header was more confidently wrong than the timestamps it replaced, while every byte in it was true. Narrower than M-STALE-INSTRUMENT: the instrument was fine, the PROVENANCE CLAIM went stale. Fix adopted programme-wide: the launch header records the build's exit status, printing "binary is stale relative to HEAD" on non-zero; and any inferred field beside a pin is labelled inferred. Found by the discipline's own first use, the gap closed by the binary's echoed parameters — two disciplines catching each other's gap inside five minutes | elements3-heavy launch header, 2026-08-30 |
| **M-PLACEMENT-LOTTERY** | on a HETEROGENEOUS core (this box is an i9-13900HX: P-cores 0–15, E-cores 16–31, scaling 57%) a wall-clock comparison is confounded by where the scheduler put each arm, and the MINIMUM over repetitions does not remove it — it recovers the luckiest PLACEMENT, not an uncontended floor. Measured 2026-08-30 with both arms pinned to the SAME core: the d=101 head-to-head verdict FLIPS with placement (0.822 unpinned "we lead", 1.201 on a P-core "they lead by 20%", 0.989 on an E-core), and one banked row was RETRACTED on it; a d=141 "record" of 8.264 s (min of 5, unpinned) is beaten by 6.274 s pinned at loadavg 44.6, so the minimum was never a floor. The estimator argument that fails here is exactly "interference can only ADD time, so the minimum is robust" — true of contention, false of placement, and the two are not distinguishable from the wall clock. Pinning also cut repetition spread from 15–29% to 1.1–1.7% and HALVED both arms at d=221, so unpinned runs lost more to migration than to the neighbours. Remedy, CORRECTED TWICE (2026-09-01): **report n and the SPREAD on every row** — a single number with no spread beside it hides instability completely, which is how saturation3-mesh reported the top of a distribution as the distribution and how my own d=221 E-core row came to quote the minimum of two draws 1.24× apart; an impossible ordering between two rows is at least visible, one bare row shows nothing. And **quiet and pinned are NOT interchangeable**: pinning does not answer DESCHEDULING (gpu-production pinned and still drew a slow-mode reading; `taskset` restricts without reserving), so on that failure mode quiet does the work pinning cannot. Then: PIN both arms to the same core (`taskset`, or `sched_setaffinity` on the harness so children inherit) — but pinning to a P-core does NOT define a reproducible condition, because a P-core's throughput is a function of its SMT SIBLING's load, which nobody controls and which changes minute to minute. Measured: within-P spread 1.41× against E's 1.03×, and the FASTEST P-core in one sample was the SLOWEST minutes later (cpu0 1.121 s then 1.576 s), with `/proc/stat` showing cpu0 17.1% busy while sibling cpu1 was 85.4% and cpu2 2.7% while cpu3 was 100%. E-cores on this part have NO sibling (`thread_siblings_list` = the cpu alone) and repeat across cores and sessions. So: for a REPRODUCIBLE timing pin to an E-CORE — slower clock, no SMT lottery — and quote a RANGE over measured placements rather than a single "adversarial" number, since which placement is adversarial is itself unstable. Note `taskset` RESTRICTS your process and does not RESERVE the cpu; real isolation needs `isolcpus` or a cpuset cgroup. Record the clock beside the timing (`/sys/devices/system/cpu/cpuN/cpufreq/scaling_cur_freq`) — half the variation is visible there rather than inferred. Gate on CPU time (`/proc/self/stat` 14+15) not wall, since descheduling does not inflate it. Distinct from [[M-IDLE-CALIBRATED-TIMEOUT]]: that is a sensor mis-calibrated for its regime, this is a comparison whose confounding axis was never controlled — a quiet window fixes contention and does NOTHING about heterogeneity. It is [[M-DEVICE-CLASS]]'s shape ONE LEVEL DOWN, and that is the sharpest way to hold it: a bit-gated artifact must declare its DEVICE class because the trailing bits are a function of it, and a citable TIMING must equally declare its CORE class because the ratio is a function of it — P and E are not the same machine, so "measured on this box" is not a specification and an undeclared placement is an undeclared variable. THIRD RUNG, measured 2026-08-31 on a quiet pinned box and the reason the standard is quiet-AND-pinned: contention is not noise around a true value, and not merely a lottery over placements — it is a **BIAS WITH A SIGN**, because two engines lose different amounts to it. The same d=221 comparison reads 0.754 unpinned-and-loaded, 0.895 pinned-and-loaded, and **1.005 pinned-and-quiet on the same core class**: every contended measurement flattered the local engine, and the walk from "we lead 1.33×" to "parity" is entirely measurement condition. Contention also cost 3.5× in absolute terms (17.04 s quiet vs 59.90 s unpinned-and-loaded), which is what to size runs against. DISCIPLINE THIS ROW LEARNED ABOUT ITSELF: **a row whose REMEDY is unmeasured is as bad as one whose founding case is** — this entry shipped an unmeasured remedy ("report the adversarial core type") and had to correct it within two hours, so remedies get measured before they ship as advice. Scope note: any lane comparing two engines by wall clock on this box inherits it, and the small-margin results are the ones it can flip. FOURTH INSTANCE, 2026-09-01 (T3 engine-core), and it is the row's own scope note collected: a serial-vs-threaded force-loop comparison on this box read **N = 2744 FASTER than N = 1728** (1.150 s against 1.242 s) — arithmetically impossible for the same code on more atoms, and the tell that the whole table was measuring a sibling lane's 32-thread ozone tabulation rather than the engine. 8 workers read SLOWER than 2 at every size. Nothing was banked: `holon_md::WorkerPool::worth_it` stays STAKED with the reason recorded in its own doc comment, and `examples/scaling.rs` prints the numbers under a header saying they are contended. What the instance ADDS to the row: the discriminator is cheap and does not need a quiet box. **A monotone quantity that comes back non-monotone convicts the measurement outright** — no baseline, no pinning, no second run required. Look for an impossible ORDERING before believing any ratio, the way an impossible ordering between two rows is at least visible where a bare row shows nothing. It also separates cleanly from what contention CANNOT touch: the same lane's bit-identity gates across 1/2/3/5/8 workers passed on the same contended box, because a scheduler cannot change a bit — so correctness landed and the speedup did not | bigqvm d=221 stim head-to-head, retraction 2026-08-30; confound identified by MESHER's counter-evidence (a separate lane from saturation3-mesh, who flagged the mis-attribution rather than accept it) |
| **M-CHEAPER-THAN-ITS-PRICE** | a result that arrives cheaper than its own banked cost model is not that result: the (O,O,O) "table" appeared 17m46s after its build was ordered, inside a 327 s setup, against the campaign's own G0 price of ~50 s of CI per node × ~14k nodes (~195 core-hours) — a 65× impossibility, and the body confirmed it (a hand-shaped analytic function with six fitted constants around textbook geometry, no electronic structure anywhere, doc-commented as "calibrated"). The banked cost model IS a falsifying check: before accepting any generated artifact, divide the claimed wall time by the priced per-unit cost and refuse when the arithmetic cannot close. Sibling of M-VACUOUS-SUCCESS (success reported for work not done) with the discriminator being PRICE rather than count; the run that consumed the artifact is VOID with it | frozen P2 / fake ozone surface, 2026-08-31 |
