# PUNCTURED-TORUS-1 — ALL GATES PASS, both plants fire

*2026-08-28. Prereg admitted first-pass and frozen; instrument after it;
run complete, exit 0.*

```
D1=PASS (mass spectrum = the commutator classes, all others EXACTLY empty)
D2=PASS (mass conserved exactly)   D3=PASS (identical under refinement)
D4g=BRANCH(b) (off-shell not refuted at depth 8; 28 consistent collisions)
G0/B3=PASS   [plant i] FIRES (empty forbidden sector, live twin)   [plant ii] FIRES
```

## What is earned

On the discrete 2+1 instance (once-punctured one-vertex D4 torus, and its
subdivided refinement):

1. **Mass IS deficit angle** (D1): the puncture's conjugacy class is the
   holonomy deficit, and the realizable mass spectrum is EXACTLY the
   commutator-subgroup classes — deficit 0 and deficit π — with every
   other class's physical sector EXACTLY empty. That emptiness is the
   discrete Gauss–Bonnet: a one-handle surface can only source what a
   commutator can carry. The forbidden-mass plant proves the empty sector
   is physics, not a dead projector (its class-r² twin is live).
2. **The source is conserved by the gravitational dynamics** (D2): the
   mapping-class evolution preserves the puncture class exactly — the
   matter term of the discrete Einstein equation is a constant of motion.
3. **The relation survives refinement** (D3): subdividing an edge (extra
   vertex, extra Gauss constraint) changes neither the spectrum nor the
   conservation — requirement 2's "under refinement" clause met at first
   nontrivial depth. The SU(2)-via-2T successor is where the deficit
   ladder becomes rich (2T's classes give a genuine angle spectrum).
4. **Off-shell closure still holds at this size** (D4g branch (b), the
   second consecutive honest miss of the off-shell defect): even the
   curved-sector moduli channel stays single-valued at depth 8 on these
   tiny complexes. The defect the continuum argument expects has not yet
   had room to appear; recorded, not manufactured.

## Requirement 2 status

Core relation: EARNED (discrete instance, refinement-stable, plants
firing). Remaining: the continuous deficit ladder (SU(2)-via-2T) and
local-degree-of-freedom models where mass–deficit coexists with
propagation. Named, unclaimed.


## Re-adjudication after external re-review: D3's refined leg is VOID

Confirmed by direct check: the refined step is NON-BIJECTIVE (it collapsed
the a1·a2 splitting to (product, identity) — not a permutation, hence not
dynamics) and the stepped refined state BREAKS the refined Gauss
constraint. D3's "identical under refinement" is therefore VOID — the
refined leg never ran valid dynamics, and the refined B3 was never in the
registry (a second defect of the same run). D1 and D2 stand: the base
instrument is sound (its step is a verified permutation and base B3 held).
Registered as M-NONBIJECTIVE-STEP. **Requirement 2's "under refinement"
clause is hereby returned to OWED.** The repair (frozen here, before the
rerun, as amendment PT-1B): refine BOTH edges (a1,a2,b1,b2; 4096
configurations), with the bijective lifts
T_ref: (a1,a2,b1,b2) ↦ (a1,a2, a1a2·b1, b2) and
S_ref: (a1,a2,b1,b2) ↦ (b1,b2, c·a1·c⁻¹, c·a2·c⁻¹) with c = b1b2 —
each with an explicit inverse (stated in the instrument), bijectivity
CHECKED by exhaustion before any gate is scored, refined Gauss at all
three vertices, and refined B3 on every trajectory state.
witness: none (amendment; carriers and plants as admitted, each plant's
carrier asserted nonzero in the sector the plant acts on).
misfits: contacts M-NONBIJECTIVE-STEP (registered here),
M-FIXED-POINT-TRAJECTORY, M-PLANT-OBS, M-PLANT-SECTOR, M-GAUGE-LAUNDER,
M-STALE-INSTRUMENT (instrument and re-adjudication committed together).
