# WILSON-2 — VOID on a misstaked null, while the substantive gates all pass

*2026-08-28. Prereg admitted and frozen; run complete; log above. Verdict
under the frozen gates: **VOID** — W3 fired and plant (ii) was unposable.
Both defects are in the freeze/instrument, and the physics underneath is
the first positive R2-line result of the programme.*

## What the data shows

- **B3 PASSES everywhere** — the covariant electric term
  `(1+ωL+ωL²)/√3` is confirmed; M-ELECTRIC-BASIS is discharged.
- **W1 PASSES** — the separated oriented triple DIFFERS between the
  dressed and off-channel carriers from step 1 on: **the
  matter-disjoint observable sees which channel drove the pump.** This is
  the result every campaign since BRIDGE-5 has chased, here on a
  non-ambivalent group with exact arithmetic.
- **W2 PASSES** — the off-carrier's triple is orientation-asymmetric
  (w₁ ≠ w₂ at every step).
- **W3 FIRED — but the STAKE was wrong, not the instrument.** The freeze
  demanded the rim triple be identical BETWEEN CARRIERS; conservation
  never implies that (two different states have different rim
  distributions). What Bianchi implies is invariance ACROSS STEPS per
  carrier — and the data shows exactly that: every rim component scales by
  exactly 3⁷ per step, ratio-exact, both carriers. The null the argument
  licenses HOLDS; the null the freeze wrote fired. Misfit registered:
  **M-NULL-MISSTAKE** (a conservation null must be staked on the quantity
  the conservation law actually constrains).
- **Plant (ii) UNPOSABLE — instrument narrower than its freeze.** The
  freeze says "first registry state with w₁ ≠ w₂"; the instrument searched
  only the dressed carrier (uniform forever, since the pump is inert
  on-channel). The off-carrier is asymmetric at step 1.
- **W4, honest note**: the class-marginal ALSO separates the carriers, so
  orientation is real (W2) but not NECESSARY for separation on this
  instance — recorded as unearned, exactly as the freeze provided.

## Amendment WILSON-2B (frozen here, before the rerun)

1. **W3′ (the correctly-staked null, EXACT)**: for EACH carrier, the rim
   weight triple at step k+1 equals the step-k triple scaled by one global
   integer factor (cross-multiplication identity on all three components).
   Fires ⇒ instrument broken. witness: none (conservation argument now
   correctly applied; the misstaked version is kept above, marked)
2. **Plant (ii)** searches the FULL registry (both carriers, all steps)
   for the asymmetric-sector carrier, per the original freeze's own text.
   Carrier sector asserted nonzero in the asymmetry, as before.
All other gates carried verbatim.

misfits: contacts M-NULL-MISSTAKE (registered by this document),
M-ELECTRIC-BASIS (discharged), M-PLANT-OBS, M-PLANT-SECTOR (plant (ii)'s
sector unchanged, its search corrected), M-LOOP-BLIND (W1's pass is its
first counterexample at separation), M-GAUGE-LAUNDER (W4's unearned note
kept), M-BARE-CHARGE, M-COND-PROBE (unchanged).
