# Pre-registration — SELECTOR-3: the observer bootstrap, forward-staked

*Frozen 2026-08-28, committed ALONE with SELECTOR_MAP.md (whose census ran
ALL CHECKS PASS in the lead session). The map's ranked recommendation: S3
is the only candidate that excludes stasis by criterion without importing
anything, HALF of it is already a theorem (Break.lean's T3 proves the
view-aligned act vocabulary powerless — the negative result is
machine-checked), and its mechanism makes a forward prediction on a
family that does not yet exist — the only support rule 6 accepts.*

misfits: contacts M-PLANT-OBS and M-PLANT-SECTOR (plants below, carriers
asserted nonzero in the sector each plant acts on), M-ONE-MODEL-DELTA
(pass/fail is the staked mechanism, never a fitted rival),
M-FINAL-VIEW-COLLISIONS (identity sets are computed by exact pair-BFS
with no word-length cut — the separation relation's own closure),
M-PROBE-EIGENSTATE and M-FIXED-POINT-TRAJECTORY (the mechanism ITSELF is
about fixed points: a macro-static sector is unobservable from inside —
the trap is here the theorem), M-NONBIJECTIVE-STEP (all dynamics verified
bijective), M-NULL-MISSTAKE (the falling-fraction stake is on the
quantity selection actually requires), M-STALE-INSTRUMENT (instrument
committed with results), M-GAUGE-LAUNDER and M-LOOP-BLIND (the holonomy
conjunct is computed as the conjugation-invariant Z₂ holonomy, never a
class marginal), M-BARE-CHARGE, M-COND-PROBE, M-ELECTRIC-BASIS,
M-RING-MIXING, M-GAUGE-UNIFORM-MOMENTUM, M-HOMOG, M-KINEMATIC-NONLOCAL,
M-VOLUME-SCALE (not otherwise contacted).

## The candidate (S3, the map's §4, frozen verbatim in substance)

A(T) = the act vocabulary T's own SECTION subsystems implement (a
T-invariant transversal's generated knobs — definable from (view, dyn)
alone; the view-aligned vocabulary is provably powerless by Break.lean T3
and is not used). T PASSES the bootstrap iff |Ident_{A(T)}(T)| = 1 over
the candidate family — the world contains its own sufficient observers.
Interpretive fence, declared here per the map: sections are Ω-definable
but not view-addressable; this freeze COMMITS to the reading that a
world's internal observers may implement acts they cannot name with
their own meter (the same commitment Break.lean made when identity became
act-relative).

## THE STAKED MECHANISM (before the instrument exists)

> |Ident_{A(T)}(T)| = 1 iff the macro map is FIXED-POINT-FREE and the
> Z₂ holonomy vanishes on every macro cycle.

Model family: n fibers of size 2, all macro cycle types, n = 4 and n = 5
(the n = 3 census that generated the hypothesis is DERIVATION, not
evidence).

## Gates (all EXACT)

- **B0**: all dynamics bijective; identity sets by exhaustive pair-BFS.
  witness: none (instrument-checked)
- **M1 — the mechanism** (two-branch): (a) the biconditional holds across
  ALL cycle types at n = 4 AND n = 5 ⇒ rule-6 support for a selection
  principle — the programme's first. (b) any deviation ⇒ the mechanism is
  size-specific; S3 drops to S2's rank; the deviating cycle types named.
  witness: vcov_preservesGauge_of_closed (the theorem half); none for the
  section half (measured)
- **M2 — THE FRACTION MUST FALL** (the decisive quantity, per the map): the
  selected fraction of worlds at n = 5 is STRICTLY below n = 4, which is
  strictly below the n = 3 derivation value (8/48). A selector picking a
  constant fraction selects nothing in the limit. Two-branch: falling ⇒
  the only upward hard-TOE evidence this document admits; flat/growing ⇒
  recorded, and the tail stays where it is.
  witness: none (measured gate)
- **N1 — S5's ceiling, the by-product null**: orbit count vs world count
  at n = 3, 4, 5. Saturating orbits against growing worlds is POSITIVE
  evidence for the no-selector null and is reported with equal prominence.
  witness: none (measured)
- **N2 — S6 scored**: the map's advance prediction that minimizing
  accidental closure selects the maximal scrambler is scored on the same
  runs. witness: none (measured; prediction frozen in SELECTOR_MAP.md §6)

## plants (carrier and sector per M-PLANT-SECTOR)

- **(i) the stasis plant**: the identity must FAIL the bootstrap at every
  n on emptiness of ACTION (all generated acts equal the identity map) —
  carrier: the identity world; sector: its generated act set, asserted
  to collapse.
- **(ii) the theorem plant**: the view-aligned vocabulary must separate
  NOTHING at every n (T3's guarantee, re-run as a live control) —
  carrier: any world; sector: the view-aligned identity set, asserted to
  be the whole macro class.
A missed plant VOIDs.

## Meaning

M1(a) ∧ M2(falling) ⇒ the observer-bootstrap mechanism is real, staked
in advance, confirmed on families it was not built on — the first
Ω-internal selection principle with rule-6 support, and the only kind of
result this programme accepts as moving the hard-TOE tail. Any other
outcome is recorded at its exact strength. Wheeler (the participatory
loop as a fixed-point condition), Deutsch–Marletto, Rovelli, and
Break.lean's T3 credited.
