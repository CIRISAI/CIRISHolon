# SELECTOR-3 — the mechanism holds, the fraction falls under a DERIVED law; the Record rider dies. A mixed result, reported as one.

*2026-08-28. Prereg and N3 rider both frozen and admitted before the
instrument existed; instrument run in the build session and re-run in the
lead session (exit contract honored: exit 2 = valid run, a staked gate
killed — the rider). Instrument and log committed with this document.*

## THE HEADLINE — the programme's first rule-6-supported selection principle

- **M1 branch (a)**: the observer-bootstrap mechanism — a world contains
  its own sufficient observers **iff its macro law is fixed-point-free
  and the Z₂ holonomy vanishes on every cycle** — holds with ZERO
  deviations across every macro cycle type at n = 4 and n = 5, families
  that did not exist when the mechanism was staked.
- **M2 branch (a)**: the selected fraction FALLS — 1/6 > 5/32 > 17/120 —
  and matches a **derived closed form**: the fraction of bootstrap worlds
  is the EGF coefficient `[xⁿ] (1−x)^{−1/2} e^{−x/2}`, → 0 as n grows,
  exact at all three n. (Stated in the instrument's own words: the law is
  a CONSEQUENCE of the mechanism, not independent evidence.)
- Both plants fire (stasis fails on emptiness of ACTION; the view-aligned
  vocabulary separates nothing, live-confirming Break.lean's T3).

Per the frozen criterion in SELECTOR_MAP.md §"what would move the
hard-TOE probability" — an advance-staked mechanism confirmed on families
it was not built on, WITH the selected fraction strictly falling under a
derived law — **the only admissible upward evidence this record defines
has now occurred.** What it supports, precisely: Ω contains at least one
internal, lawful, stasis-excluding selection principle whose selectivity
sharpens with world size. It does NOT select our universe, does not
derive T uniquely (the S5 ceiling stands), and moves nothing else.

## N1 — the ceiling census (the null, given its day in court)

Worlds grow 48 → 384 → 3840; gauge orbits grow 10 → 20 → 36. Orbits GROW,
so S5 gains no positive support — but the resolvable fraction collapses
(10/48 → 36/3840): the ceiling loosens far slower than the world count.
Selection-up-to-Ω-isomorphism remains the only coherent target.

## N2 — the S6 anti-pole, scored

The frozen prediction (minimizing accidental closure selects the maximal
scrambler) scored: argmin orders track the family maximum at n = 3, 4
([6],[8]) and fall just short at n = 5 ([10] vs 12). Direction confirmed,
exactness broken at n = 5 — recorded, not massaged.

## N3 — THE RENT-RECORD RIDER: DEAD AS STAKED (rule 7: reported as
plainly as the survivals)

Six collisions at n = 4/5 merge worlds of EQUAL macro period, so the bit
the frame-free summary loses is NOT always motion-memory. The conjecture
as frozen is falsified, killing pairs named in the log. Two things
survive, each at its exact strength: (1) the RECOVERABILITY half — the
full view-indexed profile up to gauge separates EVERY collision at every
n tested; (2) a WEAKER READING, recorded as a reading and not a result:
every equal-period collision pairs cycle types with the SAME number of
moved fibers but a DIFFERENT partition of them — the lost bit is how the
motion is DIVIDED, not whether it exists. A successor conjecture would
stake that partition-of-motion form; this document does not.
