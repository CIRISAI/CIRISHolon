# SELECTOR-3 rider — N3, the RENT-RECORD conjecture (staked before the instrument)

*Frozen 2026-08-28, before selector3.py exists, as a rider to the admitted
SELECTOR-3 freeze. Origin: the lead observed that the n=3 rent-multiset
census (8 classes, 2 collisions, 10 orbits) wears the taxonomy's own
architecture — site-generated kinds PLUS one frame-relation — and both
collisions merge a macro-STATIC world with a MOVING one.*

## The conjecture

> **The frame-relational residue of the rent classification is exactly the
> Record coordinate.** At every model size: every sorted-rent-multiset
> collision merges worlds that differ ONLY in macro-motion-memory (whether
> the macro law moves — the "can the past still be proven" axis), and the
> distinction is recoverable by view-INDEXING (keeping the frame, up to
> gauge) — never by any frame-free summary. This is
> `record_not_site_generated`'s logic recurring in Ω's cost face: the
> summary-generated classes are the artifact-local kinds; the indexing
> residue is the +1.

## Gate N3 (EXACT, two-branch, computed by SELECTOR-3's instrument at
n = 4 and n = 5 as a by-product)

- Enumerate all sorted-rent-multiset collisions between distinct
  automorphism orbits at each n.
- Branch (a): EVERY collision at every tested n merges a macro-static
  world (or lower macro period) with a strictly-higher-period world —
  the lost bit is always motion-memory ⇒ the Record recurrence is
  supported by forward prediction on families it was not read from.
- Branch (b): ANY collision merges two worlds of equal macro period ⇒
  the conjecture is DEAD as staked; the colliding pair is named and the
  residue is something other than Record.
witness: none (measured gate; the taxonomy-side anchor is the
predecessor's `record_not_site_generated`, cited as lineage, not as
evidence)

misfits: contacts M-FINAL-VIEW-COLLISIONS (the collisions ARE the
object), M-NULL-MISSTAKE (the stake is on the axis the summary provably
cannot carry), M-ONE-MODEL-DELTA, M-STALE-INSTRUMENT (rider committed
before the instrument), M-PLANT-OBS and M-PLANT-SECTOR (no new plants;
the parent freeze's plants stand, each with carrier and sector asserted
nonzero in the sector the plant acts on), M-HOMOG (contacted via the
phrase 'artifact-local'; no locality claim is staked — 'local' here is
the taxonomy's artifact-local/frame-relational split, not spatial).
