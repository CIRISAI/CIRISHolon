# Pre-registration — SELECTOR-4: the gauntlet on gauge worlds, scale-indexed

*Frozen 2026-08-28, committed ALONE. The chain's rung 2: the bootstrap
selection principle (SELECTOR-3, rule-6-supported) evaluated on GAUGE
dynamics families — and, per the lead's acuity principle, evaluated PER
OBSERVER SCALE: visible dynamics is relative to the acuity of the
observer, so the selection question is posed at every rung of a declared
acuity ladder, and "zooming" is motion along the view/act lattice. This
is the Wilsonian effective-theory picture in Ω-native form, credited to
the lead's formulation of 2026-08-28.*

misfits: contacts M-PLANT-OBS and M-PLANT-SECTOR (plants below; each
plant's carrier asserted nonzero in the sector the plant acts on),
M-FIXED-POINT-TRAJECTORY and M-PROBE-EIGENSTATE (the stasis exclusion is
inherited from the criterion, never hand-imposed), M-GAUGE-LAUNDER and
M-LOOP-BLIND (acuity rungs that read classes are DECLARED as such — the
laundering is here the OBJECT of study, since coarse acuity is exactly a
class reading), M-FINAL-VIEW-COLLISIONS (identity sets by exact pair-BFS,
no length cut), M-NONBIJECTIVE-STEP (all steps verified bijective),
M-NULL-MISSTAKE (the zoom law is staked on the quantity the coarsening
order actually controls), M-ONE-MODEL-DELTA, M-STALE-INSTRUMENT
(instrument committed with results), M-HOMOG and M-KINEMATIC-NONLOCAL and
M-BARE-CHARGE and M-COND-PROBE and M-ELECTRIC-BASIS and M-RING-MIXING and
M-GAUGE-UNIFORM-MOMENTUM and M-VOLUME-SCALE (not otherwise contacted).

## Model family

The four group-torus worlds already instrumented: G ∈ {D4, 2T, 2O, 2I}
(einstein_adm1.py / pt2t.py / pt2oi.py machinery; binary_groups.py),
state space G×G, dynamics = the mapping-class step (bijectivity verified
before any reading). THE ACUITY LADDER, declared coarsest to finest:
- A0 (blind): the one-point view.
- A1 (flatness): the commutator-trivial bit.
- A2 (mass): the conjugacy class of the puncture holonomy [a,b].
- A3 (classes): the pair (class a, class b).
- A4 (orbit): the simultaneous-conjugation orbit label (the finest
  gauge-invariant view — the ADM channel).
Act vocabularies per rung: the acts implementable by subsystems DEFINABLE
at that acuity (the instrument states the construction exactly, following
SELECTOR-3's section discipline; the view-aligned vocabulary's
powerlessness on closed views — Break.lean T3 — is the A-side control at
every rung).

## Gates (all EXACT, all two-branch)

- **B0**: bijectivity; identity sets by exhaustive pair-BFS; the acuity
  ladder verified to be a chain of coarsenings. witness: none
- **G1 — does the criterion select at all, per group per rung**: for each
  (G, Ak), the bootstrap-passing set computed exactly. Branch (a): some
  (G, Ak) selects a proper nonempty subset ⇒ the principle reaches gauge
  worlds. Branch (b): every rung selects everything-or-nothing ⇒ the
  principle's reach STOPS at fiber toys — a scope boundary, banked as
  such. witness: none (measured; the criterion's mechanism witness is
  SELECTOR-3's record)
- **Z1 — THE ZOOM LAW** (the lead's principle, staked): along the acuity
  ladder A1 → A4, the selected set is MONOTONE NON-INCREASING (finer
  observers exclude more worlds; coarser acuity forgives). Branch (a):
  monotone at every group ⇒ zooming is lawful — effective selection per
  scale nests, the Ω form of effective-theory nesting. Branch (b): any
  inversion (a world selected at fine acuity but not coarse) ⇒ the zoom
  law fails as staked; the inverting world and rung are named — itself a
  discovery about scale. witness: none (measured gate)
- **F1 — group discrimination**: does any rung's verdict DIFFER across
  the four groups (normalized: selected fraction per |G|)? Branch (a):
  yes ⇒ the first measured instance of an Ω-internal criterion
  distinguishing gauge structures — the door "particle content from
  selection" would eventually need. Branch (b): no ⇒ the criterion is
  group-blind at this family, banked. witness: none (measured)
- **FR1 — the fraction across |G|**: selected fraction at the finest
  rung across |G| = 8, 24, 48, 120 — reported with whatever law fits
  STATED AS POST-HOC unless it matches a form derived before running
  (none is; honesty clause). witness: none (measured; any law is
  post-hoc by declaration)

## plants (carrier and sector per M-PLANT-SECTOR)

- **(i) stasis**: the identity world fails at every rung on emptiness of
  ACTION; carrier: the identity; sector: its generated act set.
- **(ii) T3 control**: the view-aligned vocabulary separates nothing at
  any rung on closed views; carrier: any world; sector: its view-aligned
  identity set (must be the whole class).
- **(iii) the blind rung**: at A0 every act vocabulary is trivial and
  NOTHING is selected (selection requires acuity — the zoom axis's floor);
  carrier: any world; sector: A0's act set, asserted trivial.
A missed plant VOIDs.

## Meaning

G1(a) ∧ Z1(a) ⇒ the selection principle reaches gauge worlds and
selection NESTS across observer scale — the gauntlet's rung 2 passed,
with "zoom" now a lawful operation. F1(a) on top ⇒ gauge structures are
distinguishable by an internal criterion — the first door toward content.
Any branch (b) is banked at its exact strength. What NO outcome here
claims: leptons, groups-of-nature, kinematics, constants — the chain's
rungs 3–4 remain open regardless.
