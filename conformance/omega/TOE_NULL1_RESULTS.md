# TOE-NULL-1 — adjudicated by adversarial exchange, converged

*2026-08-28. An external review constructed 48 reversible closed dynamics
on a 6-state/3-fiber model sharing closure/λ/rent at the designated view
yet inducing three observably different macro-worlds, and initially
concluded the hard-TOE reading "already refuted." The lead session's
steelman measured the counter; the reviewer reproduced it independently,
RETRACTED the refutation and the probability cut, and strengthened the
result. Verifier: `toe_null1.py`, exact, all checks pass. Both sessions'
numbers agree.*

## The converged adjudication (the reviewer's own final wording, adopted)

1. **Single-view closure/rent summaries underdetermine dynamics** —
   measured (the 48 exist; census 8/24/16).
2. **The full Ω view-lattice spectrum repairs that underdetermination for
   the three designated witnesses** — measured: total rents 0 / 50 / 63
   over the 203-view lattice (0 / 172 / 195 nonzero views). No new face
   was added; the witnesses were never Ω-equivalent. (Scope: the three
   REPRESENTATIVE worlds — the 48 lifts carry further fiber-internal
   spectral structure, which only strengthens the point.)
3. **Separation still does not supply selection** — open, exactly as the
   record already carried it (T is input; the ADM remainder's wording).
4. **THE STASIS THEOREM (instance, exact): any selector whose sole
   objective is minimizing Ω-rent selects the identity dynamics** —
   W_v(id) = 0 on every view and rent is nonnegative, so identity
   Pareto-dominates every non-identity dynamics on the full finite
   lattice; verified: among all 720 permutations, exactly the identity is
   lattice-wide zero. Any MONOTONE rent-minimizing aggregation therefore
   selects stasis. Cost-alone selection chooses the dead world — the
   selector problem is thereby SHARPENED, not merely open: viability must
   balance maintenance against productive organization.
5. **The payer-builder selector is posed** (SELECTOR-1, prereg beside
   this document) with the frozen null argmin_T RentSpectrum(T) = id.

The hard-TOE tail returns to its prior; the cut was double-counting an
acknowledged open. Ω separates the presented worlds (measured); does not
yet select (open); pure rent minimization selects stasis (demonstrated);
W3 stands as a wager under its newly frozen extension grammar (STANCE.md).

## Scope refinement from SELECTOR-MAP (2026-08-28): view-indexed, not multiset

The selector census found the rent MULTISET incomplete on the full
48-world family: 8 distinct sorted-multiset values for 10 conjugation
orbits, with both collisions merging a macro-static world with a period-2
world (TOE-NULL-1's own period-2 witness is one half of a collision). The
three DESIGNATED witnesses' totals (0/50/63) are pairwise distinct, so
the published claim stands exactly as worded — but the general repair of
the underdetermination is carried by the **view-INDEXED rent profile up
to the automorphism group** (complete: 10 classes for 10 orbits), and
"the rent spectrum separates" must never be abbreviated to a total or a
multiset.
