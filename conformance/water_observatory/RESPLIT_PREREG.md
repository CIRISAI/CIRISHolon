# THE NEAR/FAR RE-SPLIT — FREEZE

*Frozen 2026-09-02, before the near table existed, before any gap between the tail model and
the banked curve had been computed, and before the route threshold was measured at any
radius. Every number below is either read from committed source (with its file and line),
derived from those by arithmetic shown in place, or a threshold staked here. The git history
is the check: this document lands in its own commit, ahead of the instrument's.*

**misfits:** M-VOLUME-SCALE, M-HOMOG, M-UNTESTED-GAP, M-ONE-MODEL-DELTA, M-PLANT-OBS,
M-PLANT-SECTOR, M-VACUOUS-SUCCESS, M-MAX-OVER-SUCCESSES, M-EXIT-DISCRIMINATOR,
M-SORTS-NOT-SEPARATES, M-STALE-INSTRUMENT, M-PROVENANCE-OVERREACH, M-BUDGET-LAUNDER,
M-PLACEMENT-LOTTERY, M-DEVICE-CLASS, M-CHEAPER-THAN-ITS-PRICE, M-CACHE-KIND,
M-FOREIGN-DOMAIN-CORROBORATION, M-NULL-MISSTAKE, M-BARE-CHARGE, M-LOOP-BLIND.

| id | the contact |
|---|---|
| M-VOLUME-SCALE | the deliverable IS an N-scaling threshold, and the whole point is that it is MEASURED at both radii rather than derived from the cell arithmetic. §6's G-ROUTE |
| M-HOMOG | the threshold derivation below assumes a uniform liquid density; the cell route's extent is the ATOMS' bounding box in an open or walled box, not the nominal one, so the derived N is an expectation and the measured N is the deliverable |
| M-UNTESTED-GAP | **this freeze's central risk.** The tail model's exponent was fitted over the last 10% of the O–O curve's knots (~18–20 bohr). Moving the handover to 10.24 asks it to carry 10.24–20.0, a decade it was never validated on. G-SPLIT measures that before anything is built on it |
| M-ONE-MODEL-DELTA | the far tail is ONE chosen model. A gap it passes earns "reproduces the banked table to this budget over this band", never "correct" |
| M-PLANT-OBS | every gate carries a plant re-derived for THIS instrument and pre-checked to fire; a carrier reading 0.0 REFUSES rather than scoring |
| M-PLANT-SECTOR | each plant names the sector its carrier must be nonzero in |
| M-VACUOUS-SUCCESS | the route gate asserts its WORK COUNT and the population it is drawn from: a threshold measured on a scene whose atoms never span 3 cells is a threshold about the placement |
| M-MAX-OVER-SUCCESSES | the gap verdict is a max over the band; no sampled radius drops out of it |
| M-EXIT-DISCRIMINATOR | the banked O–O curve exits `IterationCap` at 5000 iterations with worst residual 4.809e-6. Every constant fitted from it inherits that and carries it on every emitted row |
| M-SORTS-NOT-SEPARATES | G-SPLIT's band could RANK candidate radii rather than separate them; the freeze stakes what it means for NO radius to be admissible, and that outcome is a refutation rather than a retune |
| M-STALE-INSTRUMENT | one instrument commit produces every number; the near table is a NEW artifact with its own identity and provenance, never an edit of the banked curve |
| M-PROVENANCE-OVERREACH | the near table's record names what it was cut FROM and infers nothing else about it |
| M-BUDGET-LAUNDER | a refused configuration is printed as refused and never scored |
| M-PLACEMENT-LOTTERY | the route threshold is a ROUTE reading, not a timing, so placement cannot move it — stated because a threshold and a speedup are easy to conflate and only the second is confounded |
| M-DEVICE-CLASS | bit-identity claims are stated within one device class |
| M-CHEAPER-THAN-ITS-PRICE | the near table is generated by the same `generate_pair_table` door at the same knot discipline; its solver certificate is printed and is the evidence work was done |
| M-CACHE-KIND | two tables for one species pair now exist. The near table is a distinct artifact and the bank slot it occupies is part of its identity; a scene loading one must not be able to believe it has the other |
| M-FOREIGN-DOMAIN-CORROBORATION | that shortening a cutoff is standard practice in molecular dynamics is not evidence about THIS table; every number here is measured on this curve |
| M-NULL-MISSTAKE | the three conservation gates stay staked on the quantities their own laws constrain, in BOTH configurations |
| M-BARE-CHARGE | unchanged from B2: this force law carries no charge, and the re-split does not alter that or touch the ionic case |
| M-LOOP-BLIND | the gap is reported as a curve over the band and at its worst radius, never as a single averaged number that two opposite-signed regions could hide inside |

---

## 0. WHY THIS EXISTS

`Sim::derive_pair_cutoff` (`sim.rs:1584`) **floors `r_in` at the table's own last knot**: it
starts at `r_max` and only ever walks outward, so the first branch returns `r_in = r_max`
whenever the curve is already under the declared budget there. The O–O curve's `r_max` is
**20.0** bohr and `|u(20.0)| = 6.641237e-7` Ha, already under carrier-v2's `PAIR_FLOOR = 1e-6`
— so `r_in = 20.0` and `r_cut = 22.0` **at every budget a caller can declare**. `list_cutoff`
inherits it, and `CellList::rebuild` needs 3 cells per axis, so the O(N) route cannot engage
below an extent of `3 × 22.0 = 66.0` bohr.

At carrier-v2's `RHO_ATOMS = 0.014860` atoms/bohr³ that is

```
N = RHO * (3 * r_cut)^3 = 0.014860 * 27 * (r_max + 2)^3 = 0.40122 * (r_max + 2)^3
  = 0.40122 * 22.0^3 = 4272
```

**atoms before the route can engage at all** — against carrier-v2's N = 800 successor bar.
The programme is stuck behind a radius that no budget can move, because the budget is not
what sets it.

### 0.1 The design target, derived rather than chosen

Solving the same expression for the bar:

```
0.40122 * (r_max + 2)^3 <= 800   =>   r_max <= 10.59 bohr
```

H–H and O–H already stop at **10.2400**. So the staked cut is **O–O to 10.2400**, which
invents no new number, makes the near bank's support uniform, and gives a derived threshold
of `0.40122 × 12.24³ = 736`. **Derived, not measured** — §6's G-ROUTE measures it, and the
measured number is the deliverable.

### 0.2 The pairing is not optional, and this freeze does not get to assume it works

A shorter near table **without** the far sector recreates exactly the discard B1b measured
and B2 paid for. The far sector must carry `(10.24, 20.0]` — and **that is a band its tail
model has never been validated on.** `B2_RESULTS.md` §2 fitted the O–O exponent over the last
10% of the curve's knots, `p_fit = 5.0049` with residual 3.7e-5, which is a statement about
~18–20 bohr. Extrapolating it down a full decade is precisely M-UNTESTED-GAP, and it is the
first thing this freeze measures.

---

## 1. THE ARTIFACT

The near table is a **NEW artifact with its own identity**, never an edit of the banked
96-knot O–O curve.

## 1bis. AMENDMENT, 2026-09-02 — three of §1's options and one of §0's numbers were wrong

*Landed before any G-SPLIT result was read. Every change below NARROWS the design or
CORRECTS a fact about the setup; none moves a threshold, and none makes anything easier to
pass. The cause is `saturation2-water`'s measurements on the curve they own, which is exactly
the review this freeze asked for.*

**(1) The RESAMPLED form is measured and REJECTED, not offered.** `table::grid_point` places
knots at `u = r_min^(−1/4) + (r_max^(−1/4) − r_min^(−1/4))·i/(n−1)`, `r = u^(−4)` — so
**`r_max` appears in every interior knot's position**. Regenerating at a shorter `r_max`
moves all of them: between the 20.0 grid and a 10.24 grid at the same `n` the number of
shared interior knots is **ZERO** (knot 10 sits at 1.873169 on one and 1.795747 on the other).
There is no bit-referee down that road, so the resampled form is struck. **The near table is a
TRUNCATION of the banked grid**, generated on the banked rule (`r_min = 1.5261`,
`r_max = 20.0`, `n = 96`) and cut after the last knot at or below the target.

**Its consequence, stated here rather than left to be tripped over:** the near table's grid
rule cites `r_max = 20.0` while its support ends near 10.30. That is odd-looking and it is
the price of the bit-referee; the artifact must say so on its face, because a grid rule
referencing a radius the table does not reach reads as a bug two months later.

**(2) The cut radius is 10.2972, not 10.2400.** 76 of the 96 knots lie at or below 10.24 and
the nearest knot is `i = 76` at **r = 10.2972**. The freeze's §0.1 target was `r_max ≤ 10.59`,
which 10.2972 clears, so the design is unchanged — but the derived threshold moves from 736
to `0.40122 × 12.2972³ = 746`. The instrument's ladder keeps 10.24 as a probe point; the
ARTIFACT is cut at the knot, because a table cannot end between its own knots.

**(3) The disclosure is "IterationCap because DEGENERATE", not merely IterationCap.** All
twelve iteration-capped knots span **6.1651 to 8.5269 bohr and ZERO lie above 10.24**, and
nine of them are capped because the O–O multiplet has no gap at dissociation — the curve is a
triplet at its well and crosses spin three times (cryo-HO's scan). **No budget and no
precision tier converges them**; the fix would be spin-sector targeting or state averaging,
a different method. So G-SOLVE's rows carry the reason and not just the exit, which stops a
reader wondering whether budget 10000 would help.

**Two consequences run opposite ways and both are recorded.** The band this design hands to
the far sector, 10.24 → 20.0, is **entirely converged**, so G-SPLIT is a fair test of the
tail model rather than a test of the curve. And **the unconvergible region stays entirely in
the near table** — cutting at 10.2972 removes none of it, and all twelve capped knots are
inherited by the artifact this campaign banks.

**(4) The near table is ADDITIONAL and the default does NOT move.** Six files build the O–O
curve, and the two that matter are the quench arms (`waterquench.rs`,
`waterquench_traj.rs`), whose bond criterion reads `outer_turning_point` — which searches the
tail. **Banked results depend on the default being the full-length curve.** So this campaign
adds an artifact and changes no default; the quench arms moving to a short table would be a
separate decision with its own re-bank, never a side effect of this split. `b2_longrange.rs`
is on that list too and keeps the full-length curve, which is correct: B2's own measurements
are against the long curve by construction.

**What the amendment does NOT change:** G-SPLIT's budget of 1.0e-7 Ha, its candidate ladder,
the `r_max ≤ 10.59` target, and every branch in §4. The one number that moves is a derived
expectation (736 → 746), and G-ROUTE measures it either way.

---

## 2. THE GATES

- **G-SPLIT — THE ADMISSIBLE HANDOVER RADIUS, and the gate that can refute this design.** For candidate `R_s` on the staked ladder `[10.24, 11.0, 12.0, 13.0, 14.0, 15.0, 16.0, 18.0, 20.0]` bohr, the tail model is rebuilt at that `R_s` and `max |u_far(r) − u_table(r)|` is measured over `(R_s, 20.0]` at 200 sampled radii. A radius is ADMISSIBLE when that worst gap is below `1.0e-7` Ha — one tenth of carrier-v2's own `PAIR_FLOOR`, so a pair the far sector carries wrongly is worth less than a tenth of the error the near sector is already allowed to drop. The gap is printed for EVERY candidate, admissible or not. witness: `DependsWithinUpTo`
- **G-EXACT — THE ZERO-DISCARD REFEREE.** On B1b's own admitted frames, under its own manifest refusal, the re-split configuration's total pair interaction is compared against the FULL-TABLE complete sum, per frame. Three regions, gated separately because they fail differently: pairs at `r <= R_s` must agree EXACTLY — bit-identical, since §1bis struck the resampled form and the near table is a truncation of the banked grid whose every retained knot is the banked curve's own; pairs in `(R_s, R_f]` within the G-SPLIT budget of 1.0e-7 Ha per pair; pairs past `R_f` counted and reported as the residual discard, gated at `0.10·D_s` exactly as B1b gated its own. witness: `DependsWithinUpTo`
- **G-ROUTE — THE DELIVERABLE.** The smallest N at which `Sim::route()` reports `Cells`, MEASURED by bisection on a 3D liquid-density scene at the banked radius and at the re-split radius, with the cell arithmetic (`cells_per_axis`, extent, cutoff) printed at every probe. A ROUTE reading and not a timing, so placement cannot move it. Both numbers are reported beside their derived expectations of 4272 and 736; a measured value differing from its derivation by more than 20% is REPORTED as a finding about the derivation, not silently accepted. witness: `none (a measured property of the engine's own decomposition; M-VOLUME-SCALE is its warrant)`
- **G-LAWS — THE THREE LAWS IN BOTH CONFIGURATIONS.** Energy, momentum and angular momentum, each staked on the quantity its own law constrains, run at the banked radius and at the re-split radius over at least 20000 steps. A re-split that buys the route by breaking a conservation law has bought nothing. witness: `none (conservation gates on this engine's own ledgers; M-NULL-MISSTAKE is its warrant)`
- **G-LEGAL — THE REFUSALS STILL FIRE.** The re-split lowers `legality_radius`, so boxes that were illegal become legal. Every refusal is re-exercised at the new radius: sub-support (R3), the periodic floor, `scale_box`'s door. A refusal that stops firing because the radius moved is a refusal that was measuring the radius. witness: `none (a refusal-observability gate; the cryo finding is its warrant)`
- **G-WORK — WORK COUNT.** Printed for every arm: candidate radii probed, sampled gap radii, frames scored, N values bisected, steps integrated. A gap measured over an empty band and a threshold measured on a scene that never spans 3 cells are the same failure. witness: `none (an anti-vacuity assertion; M-VACUOUS-SUCCESS is its warrant)`
- **G-SOLVE — THE NEAR TABLE DID WORK.** The near table prints `route`, `exit`, `n_det`, `n_basis`, `solver_budget` and `worst_residual`, and the banked curve's own `IterationCap` / 5000 / 4.809e-6 travel beside every constant derived from either. witness: `none (a work-evidence and disclosure gate; M-CHEAPER-THAN-ITS-PRICE and M-EXIT-DISCRIMINATOR are its warrants)`

## 3. PLANTS

**P-GAP — against G-SPLIT.** Perturb the fitted exponent by 1%. *Carrier:* the change in the
worst gap over the band. *Sector:* the tail model. Nonzero because the gap depends on `p`
through `r^(−p)` over a decade, so a 1% exponent error is a factor of `10^0.01·p ≈ 1.12` at
the band's far end.

**P-BAND — against G-EXACT.** Drop the far sector's contribution over `(R_s, r_max]`
entirely. *Carrier:* the omitted band's energy. *Sector:* the far pair sector. Nonzero because
B1b measured that band at up to 1.150526e-5 Ha, which is the number this whole design exists
to keep.

**P-ROUTE — against G-ROUTE.** Force the complete route (`RoutePolicy::Complete`).
*Carrier:* the measured threshold. *Sector:* the decomposition. The bisection must fail to
find any N, rather than reporting a large one — a threshold search that returns a number on a
scene that can never take the route is measuring its own upper bound.

**P-LAWS — against G-LAWS.** The non-central far force from B2's P3, unchanged. *Carrier:*
`|L − L₀|`. *Sector:* angular. It must fire the angular gate while the momentum gate stays
green, exactly as it does in B2's battery — if it stops doing that under the re-split, the
re-split changed something the freeze did not intend.

**P-LEGAL — against G-LEGAL.** A periodic box between the two legality floors: legal under
the re-split radius, illegal under the banked one. *Carrier:* the differing verdict.
*Sector:* the legality radius. This is the plant that proves the two radii are actually
different where it matters, and it is the one that would catch a "re-split" that did not
change what the engine believes.

## 4. THE BRANCHES

* **BRANCH (a) — THE CUT LANDS.** G-SPLIT admits `R_s ≤ 10.59` and every other gate passes.
  → The re-split delivers the route threshold; **G-ROUTE's measured N is the headline**, and
  carrier-v2's N = 800 bar is O(N)-reachable. The tail model's gap over the band is quoted
  with the verdict every time, because that is what was bought with it.
* **BRANCH (b) — PARTIAL.** The smallest admissible `R_s` is above 10.59 but below 20.0.
  → The table can be shortened but **not far enough for the N = 800 bar with THIS far model**.
  Report the achievable threshold, the radius that achieved it, and name what a different far
  model would have to do. This is a result, not a failure to get one.
* **BRANCH (c) — REFUTED.** No `R_s < 20.0` is admissible. → **The power-law tail cannot carry
  any of the band**, the near/far split as built cannot buy the route, and the design is
  reported dead with the measured gaps that killed it. The near table is not banked.
* **BRANCH (d) — VOID.** Any of §5 fires.

**Pre-committed follow-up, designed in rather than rescued.** If (b) or (c), the same
instrument re-runs with the far model's exponent fitted over the WHOLE band rather than the
last 10% of knots — a different model, declared as one, gated at the same budget, and reported
as a second model rather than as a correction to the first. No new budget, no re-chosen band.

## 5. VOID CONDITIONS

- **V1 — PLANT DID NOT FIRE.** Any plant whose carrier reads 0.0, or which fires the wrong gate.
- **V2 — THE ARTIFACT IS UNDECLARED.** The near table does not state its form (prefix or
  resampled), so G-EXACT cannot know which comparison it is entitled to.
- **V3 — EMPTY BAND.** G-SPLIT's band contains no sampled radius, or G-EXACT's frames contain
  no pair in `(R_s, r_max]`.
- **V4 — WORK FLOOR.** G-WORK fails on any floor.
- **V5 — THE ROUTE NEVER ENGAGES.** G-ROUTE's bisection finds no N at either radius, so there
  is no threshold to compare and the deliverable does not exist.

## 6. WHAT THIS CANNOT SAY

1. **Nothing about ionic scenes.** Unchanged from B2; the force law still carries no charge.
2. **Nothing about a speedup.** G-ROUTE measures where the route ENGAGES, not what it costs
   once it does. That the cell route is cheaper than the complete one at large N is B2's
   G13 territory and is not re-measured here.
3. **Nothing about the banked curve's correctness.** The near table is cut from it; every
   defect in the original is inherited, including its budget-limited solve.
4. **Nothing above the densities and box shapes G-ROUTE probes**, and the derived expectations
   of 4272 and 736 assume a uniform liquid density in a box whose extent the atoms actually
   span — which M-HOMOG says is exactly what a bounding-box extent may not do.
