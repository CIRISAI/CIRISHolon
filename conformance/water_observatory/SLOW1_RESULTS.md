# SLOW-1 — READ: a RE-FINDING. The slow structural sector of the sluggish operator is tetrahedral order (loading 0.82–0.88 on three seeds), it carries mobility at 293 K and not at 400 K, and it misses both numeric bars — no branch cleanly; the physics lane closes as method

*2026-09-25. Prereg `SLOW1_PREREG.md` (frozen alone `53dcecc`), Amendment 1 (`473f655`, the
literature's candidates named), reader `slow1_search.py`, read `replace0/slow1/slow1_read.txt`
(written unattended by `conformance/monitor/slow1_autoread.sh` at 02:42 CDT). Carrier: the
rigid operator, 250 waters, 50 ps at 20 fs, three seeds at 293 K and one at 400 K.*

## 0. Verdict

| stake | staked | read | verdict |
|---|---|---|---|
| **S1** a closed structural sector at lag ≥ 1 ps | own σ₁ ≥ 0.5, kill < 0.2 | σ₁ = `0.349, 0.301, 0.332` at 1 ps on the 293 K seeds; `0.068` at 400 K | **between** — it closes, less than staked |
| **S2** the sector carries mobility | +0.05 R² beyond velocity and each candidate at 293 K, less at 400 K; kill < 0.02 | `+0.0283` on every 293 K seed; `+0.0002` at 400 K; beyond velocity alone `+0.039` against `+0.0014` | **between**, with the temperature contrast clean (a factor of ~140) |
| **S3** direction or re-finding | loading ≥ 0.8 on one candidate ⇒ re-finding | tetrahedral order `q`: **`0.863, 0.817, 0.880`**; every other candidate ≤ 0.16 | **RE-FINDING on all three seeds** — the literature's candidate, confirmed on this operator |
| α₂ check | the non-Gaussian peak within 2× of τ* | peak `3.9–4.8` ps against τ* = 1 ps at 293 K (inconsistent); `0.76` ps at 400 K (consistent) | the sector's lag is shorter than the heterogeneity's |
| plants PS-1, PS-2, PS-4 | as frozen | pass on all four arms (momentum σ₁ ≤ 0.007; re-paired null ≤ 0.0005; time-shuffle ≤ 0.10) | **all fire** |

**Branch.** None cleanly by the letter (S1 and S2 between their stakes and kills); by S3 the read is
**(b)**: with the plants firing, the search, told nothing, returned tetrahedral order as the
slow structural variable of the sluggish liquid — the field's answer (Errington–Debenedetti's
`q`; its governing role in water's slow domains reported in the literature Amendment 1 cites).
The one quantity beyond the re-finding: the sector carries a small, clean increment to
five-picosecond mobility at 293 K (`+0.028–0.040` R²) that vanishes at 400 K, measured with a
re-paired null — a closed-and-carried statement the literature asserts more often than it
tests. It is a statement about this operator, with a linear dictionary.

**What this decides.** By the plan agreed on 2026-09-22, a re-finding closes the physics lane
as method: the fifth carrier on which the procedure returned what physicists already knew.
