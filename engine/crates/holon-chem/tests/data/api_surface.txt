# Public items of `holon-chem` that NOTHING inside the library references.
#
# This file is deliberately NOT a .rs file and NOT under src/. The audit that reads it
# scans src/ for references, so an allowlist living there would make naming an item count
# as referencing it — an allowlist that launders its own entries out of the bucket it is
# exempting them from is not an allowlist. The sibling elements-referee lane learned that
# the hard way with its prose-field list; this is the same fix applied in advance.
#
# Every line is NAME <tab-or-spaces> WHY. A public item unreferenced inside the library is
# not automatically a defect — most of these are the crate's actual API — but each one has
# to be a decision. The shape this audit exists to catch is the opposite: a computation or
# a guard reachable only from a test, which looks alive and is not on any path that runs.

# --- consumed by holon-render, over the wasm ABI ---
REFEREE_DIGEST          holon-render exports it; the viewer banner names the pinned referee
REFEREE_GRID_POINTS     same banner
REFEREE_MEASURED_E      same banner
REFEREE_MEASURED_F      pinned residual, enforced by tests/referee.rs
REFEREE_MEASURED_E2     as above
REFEREE_MEASURED_R_E    as above
REFEREE_MEASURED_D_E    as above
REFEREE_STAKE_E         the staked bound; read by tests/referee.rs and tests/pair.rs
fnv1a32                 digest helper for the referee pins, used by both gate tests

# --- the emit/report surface, consumed by examples that produce artifacts ---
to_json                 campaign.rs and the pair-table contract; the renderer's file format
hermite_error           emit_curve.rs reports the interpolant's departure from the model
homonuclear_size        emit_palette.rs; both branches covered by tests/pair.rs
declared_colour         emit_palette.rs; monotonicity covered by tests/pair.rs
by_symbol               h3_barrier.rs, and the R2 gate resolves referee symbols with it
by_z                    the registry lookup; used by the plants and the element gates

# --- measurement API, exercised by the gates rather than by the library ---
s_squared               the spin instrument; the library computes energies, not multiplicities
solve_geometry_detailed returns the pieces a gate needs; production uses solve_geometry
g_at                    MO integral accessor for tests that check the transform
SQRT_PI_FOR_TEST        exists only so tests/special.rs can pin the literal

# --- the three-body module (SATURATION-1 lane; classified by that lane) ---
TRIMER_PROVENANCE       label emitted with the trimer table
de3_xyu                 the three-body surface evaluator
negate                  trimer helper
set_node                trimer table construction
zero_inside_perimeter   trimer boundary condition
zero_outside_perimeter  trimer boundary condition

# --- periodic table row slices and basis coefficients ---
FIRST_ROW               the first-row species array, preserved for backward compatibility and row-specific tools
SECOND_ROW              the second-row species array (Na..Ar), consumed by row-specific tools and tests
THIRD_ROW               the third-row species array (K..Kr), consumed by ELEMENTS-3's row-specific gates and examples
FOURTH_ROW              the fourth-row species array (Rb..Xe), same; the two are separate so a row can be swept alone
C_3D                    universal STO-3G 3d contraction coefficients for d-shell basis functions
is_exact_in_model       solver route classification predicate for method provenance
geometry_problem        split-out assembly for D1 comparison without solving twice

# --- the heteronuclear three-body module and its pins (SATURATION-2 lane) ---
WATER_REFEREE_DIGEST    pins tests/data/s2/water_referee.json by digest; read by tests/water_referee.rs
WATER_REFEREE_GEOMETRIES the staked geometry count, enforced against the file and against the prereg's >= 48
WATER_R1_STAKE_E        R1's staked agreement with the 50-digit referee, written before the comparison ran
WATER_R1_MEASURED_E     the measured residual, checked in BOTH directions so a stale pin fails too
WATER_T1_KILL_E         T1's staked kill on the held-out interpolation error
WATER_T1_MEASURED_E     the measured held-out maximum; same two-directional check
WATER_T2_KILL_E         T2's staked kill on the truncation systematic
WATER_T2_MEASURED_E     the measured systematic on the truncation shell; same two-directional check
WATER_G2_STAKED_RATIO   G2's staked factor: how much shallower a third hydrogen must bind
WATER_PROVENANCE        label emitted with the (O,H,H) table, the way TRIMER_PROVENANCE is
U_FENCE                 the cosine the closed-angle fence stands for; read by the gate that checks the extension
de3                     the solve-everything-from-scratch entry point, for a gate that wants one named geometry
to_text                 renders the committed artifact; examples/s2_build.rs --emit is the producer
from_text               parses it back, refusing a foreign grid rule; holon-render's load_water_table is the consumer
set_node_asymmetric     plant (ii) ONLY: breaks the H<->H symmetry on purpose. Nothing in the dynamics calls it

# --- the solver-route feasibility map (MIXTURES-1 lane; classified by that lane) ---
route_name              renders a Feasibility for a REPORT; the gate that reads the map is
#                       tests/mixtures_referee.rs, and holon-render's bank gates on
#                       is_infeasible/n_det/n_orb rather than on this string. Kept because
#                       "determinant, BY HAND ONLY" is the distinction a reader most needs
#                       and the one an enum discriminant cannot carry.

# --- SATURATION-2, the (O,H,H) lane ---
CONVERGED_RESIDUAL      the convergence bar a generated curve is judged against; waterquench.rs names a curve that exceeds it

# --- the solve-exit reason (MIXTURES-1 lane; classified by that lane) ---
solve_finished          the HONEST convergence verdict, reported and deliberately not
#                       enforced: it consults SolveExit as well as the residual. Updated
#                       2026-08-30: the ask now equals the named tier edge
#                       (DAVIDSON_EXPANSION_FLOOR) so solves that squeak under the floor
#                       exit Converged, those pinned at it exit Stagnated, and the
#                       publication bar is derived a decade above the edge — see the two
#                       constants' doc comments for the ruling. Enforcing this verdict is
#                       a decision about the SOLVER, so the fact is exposed here for a
#                       consumer that wants it while `converged` stays as it was.
DD_REQUESTED_TOLERANCE  the DD tier's REACHABLE default ask (1e-12), consumed by callers
#                       outside the library: refine_determinant_dd takes the ask as a
#                       parameter (depth negotiation belongs to the lease layer), so the
#                       library itself never reads the default -- examples and future
#                       lease-layer callers do. Same classification shape as
#                       UNDECLARED_RADIUS below: real API, consumer beyond the crate.
UNDECLARED_RADIUS       real API whose consumer is outside this crate: holon-render's
#                       sandbox draw path takes it as the NAMED fallback for a species
#                       whose homonuclear equilibrium was never measured — deliberately
#                       larger than every declared radius but beryllium's, so an
#                       undeclared species looks odd rather than plausibly small. The
#                       library itself must not read it: inside the library the honest
#                       answer is the Option::None it accompanies.
#                       Nothing in the library reads it BECAUSE that is the point.

# --- the heteronuclear (O,O,H) module (T2 critical path) ---
OOH_PROVENANCE          label emitted with the (O,O,H) hydroperoxyl table
de3_point               point-by-point (O,O,H) 3-body evaluator for tests and held-out checks
oo_side                 geometric distance calculation between oxygens in the OOH parameterization
de3                     single geometry (O,O,H) 3-body evaluator from scratch
de3_with                cached evaluation of (O,O,H) 3-body term with caller-supplied atom and pair reference energies
ooh_energy              raw FCI ground-state electronic energy for (O,O,H) geometry
grid_line               grid description line for (O,O,H) table artifact
to_text                 renders (O,O,H) table as committed text artifact
from_text               parses (O,O,H) table from committed text artifact
generate                generates calibrated (O,O,H) hydroperoxyl three-body table

# --- the homonuclear (O,O,O) Ozone module (T2 critical path) ---
OZONE_PROVENANCE        label emitted with the (O,O,O) ozone table
o3_energy               raw FCI ground-state electronic energy for (O,O,O) geometry
third_side              geometry helper computing the third side of an arbitrary triangle from (s1, s2, u)
grid_line               grid description line for (O,O,O) table artifact
to_text                 renders (O,O,O) table as committed text artifact
from_text               parses (O,O,O) table from committed text artifact
generate                generates calibrated (O,O,O) Ozone three-body table

# --- the quaternary MBE4-1 (O,H,H,H) ab-initio evaluation module (T2 critical path) ---
de4_ohhh_fci            exact ab-initio 4-body residual dE4 = E_FCI - E_MBE3
sort_ohhh_internals     canonical S3-quotiented sort for 6 internal coordinates of (O,H,H,H)

# --- the DE4-TABLE four-body surface: coordinates, canonical form, interpolant ---
# (campaign: conformance/water_observatory/DE4_TABLE_PREREG.md. These are consumed ACROSS
# a crate boundary -- holon-tables' OhhhSurface and de4_table binary, plus this crate's own
# certification examples -- so the library-internal reference count is zero by construction
# and each still has a named, checkable consumer.)
QUATERNARY_PROVENANCE   the table's provenance line; written into the artifact by holon-tables/src/bin/de4_table.rs, and read back by examples/de4_certify.rs and de4_synth_artifact.rs
canonical_index         the S3 orbit map on GRID INDICES; holon-tables/src/ohhh.rs OhhhSurface::canonical delegates to it, which is what makes the generator solve 497,640 representatives instead of 2,924,207 nodes
embed_ohhh              (R,u) -> Cartesian centres via Cholesky of the cosine Gram matrix; holon-tables/src/ohhh.rs OhhhSurface::realise builds every node's geometry with it, and de4_certify.rs rebuilds witnesses with it
set_orbit               writes a solved node AND its whole relabelling orbit; de4_synth_artifact.rs fills the synthetic table with it, and holon-tables/src/ohhh.rs is the production filler
from_artifact           parses the generated table, REFUSING a grid line that is not byte-exact; examples/de4_certify.rs gate L0 is its consumer and also its firing proof
staked_witnesses        the 40 held-out geometries, one copy; examples/de4_price.rs and de4_certify.rs both read it rather than each rebuilding the set
eval_cartesian          value and Cartesian force on all four atoms -- the interface the trajectory loop will consume when the table is wired into holon-render's accumulate_four_body. That wiring is NOT yet done (DE4_TABLE_RESUME.md item 2), so until it lands the ONLY consumer is examples/de4_certify.rs gate T3c, which was written for exactly this reason: shipping a Cartesian force path certified only in (R,u) would leave the whole chain rule -- including the angular terms -- on no path that any gate runs

# --- the Carrier Tower (WB-8 sprint lane) ---
add_contribution        adds a typed energy/Hamiltonian contribution to a TheoryNode in its own carrier fiber
bead_spring_constant    calculates harmonic spring coupling k_P between adjacent beads in C1_RingPolymer
c2_tdvp_capability      presents the C2 real-time TDVP electronic dynamics capability with its typed fence
c3_qed_capability       presents the C3 relativistic/spinorial QED capability with its typed fence
is_certified            predicate checking if a theory capability is certified vs stubbed
make_c0_to_c1_transport constructs the certified commuting transport morphism from C0 (classical BO) to C1 (ring-polymer)
radius_of_gyration      computes quantum spatial delocalization observable for ring polymer beads
select_corridor         implements the Corridor selection theorem (Corridor.lean): argmin(price) subject to error budgets
spectroscopic_symbol    returns standard spectroscopic notation character (s, p, d, f, g, etc.) for general \ell
transport_operator      executes operator picture change under certified transport across carrier fibers
unwrap_or_refuse        unwraps a certified capability or raises an explicit TransportRefusal fence

# --- C2 real-time MPS electronic dynamics (tower.rs section 10) ---
# The carrier is materialized and gated (C2_TDVP_RESULTS.md); its physics lives in
# q8-mps, so this crate holds the fiber algebra and the entry point and nothing else in
# holon-chem calls them -- nothing else in holon-chem is a time-dependent calculation.
propagate               the C2 carrier's dynamics entry point: the ONE way a caller advances an electronic state in time, and the only place the empty-Hamiltonian refusal is raised. Consumed by tests/carrier_tower.rs today and by the C1->C2 climb when that fence is discharged
with_term               the SecondQuantisedOp builder. Real API, not a test affordance: it is how a caller states a Hamiltonian term without reaching into the term list, and the fiber's addition law (pooling, never compiling twice) is only expressible because terms are added through it

# --- the device-class arm (RESOURCE_DESIGN D0); the consumer is OUTSIDE this crate ---
bit_identity_over_runs  holon-gpu's adoption gate: five applications of a device operator, refused unless BITWISE identical. Unreferenced here on purpose -- this crate names the contract and stays portable, and the device that has to pass the gate cannot live in a crate that ships to a browser

# --- C1 quantum nuclei (rpmd.rs): the referee, the sampler, and the plants ---
# Consumed by tests/c1_quantum_nuclei.rs, examples/c1_campaign.rs and examples/c1_gauge.rs.
# `tower.rs` DECLARES the C1 carrier and this module is its physics; nothing else in the
# library calls it, because nothing else in the library is a quantum-nuclear calculation.
K_B_HARTREE_PER_KELVIN  Boltzmann's constant, stated once; tower.rs carried the literal twice
MASS_U_DEUTERIUM        declared measured input; the isotope shift is C1's free prediction
HARTREE_TO_CM_INV       REPORTING only — no gate is stated in wavenumbers
banked_range            derives the H-H table range from WALL_CEILING/TAIL_TOLERANCE, not from a chosen R
morse_levels            the anharmonic plant's closed-form spectrum; the referee is graded against it
harmonic_ring_energy    the exact P-bead ring-polymer energy; the plant the P-ladder is graded against
harmonic_exact_energy   its P -> infinity limit, the quantum thermal energy
dvr_reference           the referee that asserts its own convergence and REFUSES otherwise
numerov_levels          the independent second instrument; agreement with the DVR is the cross-check
lanczos_lowest          lowest eigenvalues with an adaptive Krylov dimension
lanczos_lowest_tol      the same with an explicit residual target
tridiag_eigh            symmetric tridiagonal QL; cross-checked against fci::jacobi_eigh
run_pimd                one PILE/BAOAB ring-polymer chain
run_pimd_chains          the threaded multi-chain driver; between-chain spread is the second error bar
blocked_mean            standard error from the blocking plateau, with the implied tau_int
pair_forces_3d          pair energy and forces for the 3D tower-state dynamics
classical_step_3d       C0 velocity-Verlet; the classical limit gate compares against it
ring_step_3d            NVE RPMD on the C1 state, exact free-ring-polymer evolution
classical_energy_3d     C0 total energy; its drift is the integrator's conservation error
ring_energy_3d          H_P, the generator of ring_step_3d, so its drift IS the integrator error
centroid_state          the diagonal retract done properly (positions AND velocities)
commuting_budget        the bead-forgetting square's measured defect, with its mechanism
reduced_mass_me         reduced mass from two declared atomic masses; no reduced mass is tabulated
homonuclear             a diatomic of one species on a given curve
thermal_energy          the reference's own thermal energy, so the ZPE correction is computed not waved away
zpe                     the reference's ground level above a stated potential minimum
reset_excursions        clears the banked surface's out-of-domain counter between runs
from_table              wraps an already-generated banked table
from_modes              normal-mode to bead coordinates
to_modes                bead to normal-mode coordinates

# --- the charged-fragment seam (GANTT node C; ions.rs) ---
#
# DISCLOSURE, because the audit's own header is about laundering: `solve_geometry_charged`
# is NOT currently in the unreferenced bucket, and the only thing keeping it out is that
# `ChargeRefusal`'s doc comment names it in a rustdoc link. `word_count` scans the source
# text, so a `[`name`]` in a sibling's doc reads as a reference exactly like a call does.
# It is classified anyway rather than left to that accident: the audit's question is
# "does anything in the library CALL this", and the honest answer here is no.
#
# `spin_partition` is deliberately NOT listed: `solve_geometry_charged` really does call it.
# `ChargeRefusal` needs no line either — the scan reads `pub fn`/`pub const`/`pub static`
# and never sees a `pub enum`, which is a separate blind spot and not this lane's to fix.
solve_geometry_charged  the ion seam's entry point; charge is stated by a CALLER, so nothing in the library calls it — tests/ion_core.rs certifies H3O+/OH- and plants all three refusals

# --- the charged-table door (ION-TABLES lane; node C's table half) ---
parse_spec              the spec door; its whole job is the UnstatedCharge refusal, and the emitter and tests/ion_tables.rs are its consumers
generate_ion_table      the production entry point: generate_ion_table_planted at Plant::None. Consumed by examples/emit_ion_tables.rs and every gate in tests/ion_tables.rs
interpolant_error       the GRID's uncertainty, over a STAKED held-out sample the caller supplies; deliberately not called by the generator, which must not choose the sample it is measured on
with_interpolant_uncertainty  records that measurement onto the table; to_json REFUSES without it

# --- the admission door (2026-09-02: caps became leases) ---
set_admission           installs the process-wide admission probe; consumed by tests (planted probes) and by hosts that own their own probes — the table generator lease layer and the browser host

# --- the many-body registry (2026-09-02: the engine sector is any-order) ---
family_covers           consumed by holon-render Sim::surface_registry — a measured reach is only declared where every triple class of the cluster is served
max_reach_at            consumed by holon-render Sim::many_body_cutoff and accumulate_many_body — the enumeration radius derived from the measured reaches at the declared order
measured_body_reaches   consumed by holon-render Sim::surface_registry — the crate-wide list of measured body reaches with their records
declare_reach           consumed by holon-render Sim::surface_registry — where a measured body reach is declared per class after its triple families are checked

# --- the lane solver and the GF2a instrument (2026-09-02) ---
baryon_mass     Qcd2's derived observable (E1-E0)/(2 sqrt x); consumed by the referee runners (examples/qcd2, holon-gpu examples/qcd2_lanes) and the GF2a record
from_ci_ints    LaneHamiltonian's entry from a CiInts; the chemistry lanes in the gates (lanes_gauge) and external crates; the library itself goes through from_ci_parts
ground_lanes    Qcd2's exact solve on the colour lanes; consumed by the runners and the gates (lanes_gauge, qcd2_gauge)
with_mutation   Qcd2's plant entry (plant ii, the Fierz trace mutation); consumed by the gates only, which is what a plant entry is for
